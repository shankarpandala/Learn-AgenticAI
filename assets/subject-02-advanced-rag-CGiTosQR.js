import{j as e}from"./vendor-Cs56uELc.js";import{C as r,S as t,B as n,N as s,W as i,A as o}from"./content-components-CDXEIxVK.js";function a(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"HyDE — Hypothetical Document Embeddings"}),e.jsx("p",{children:"Standard RAG embeds the user's question and searches for documents that are semantically close to it. This works well when questions and documents are phrased similarly, but breaks down when a short, ambiguous question is far in embedding space from the verbose, technical document that actually answers it. HyDE bridges this gap by generating a hypothetical answer first and using that as the search query."}),e.jsx(r,{title:"HyDE (Hypothetical Document Embeddings)",definition:"HyDE is a retrieval technique that asks a language model to generate a hypothetical answer to the user's question — without access to any documents — and then embeds that hypothetical answer rather than the original question. The intuition is that a plausible answer, even if factually incorrect, will be phrased in the same style and vocabulary as the real answer documents, making it a better semantic match."}),e.jsx("h2",{children:"Why HyDE Improves Retrieval"}),e.jsx("p",{children:`Consider the query: "What causes the aurora borealis?" This 5-word question sits in a different region of embedding space from a 300-word Wikipedia paragraph about solar wind and magnetospheric physics. A hypothetical answer generated by an LLM — "The aurora borealis is caused by charged particles from the sun interacting with Earth's magnetic field..." — uses the same terminology and sentence structure as the real document, closing the embedding space gap.`}),e.jsx("p",{children:"HyDE is especially effective for: domain-specific technical corpora where question phrasing differs sharply from document phrasing, scientific literature retrieval, legal document search, and low-resource domains where sparse training data means embedding models have weak coverage of the domain vocabulary."}),e.jsx("h2",{children:"When HyDE Hurts"}),e.jsx("p",{children:"HyDE assumes the LLM can generate a plausible hypothetical answer even without grounding. If the LLM has no knowledge of the domain, its hypothetical answer will be generic or wrong — and may retrieve less relevant documents than a direct question embedding. Test HyDE empirically against your corpus before adopting it; it is not universally beneficial."}),e.jsx(t,{title:"HyDE Implementation",tabs:{python:`import anthropic
import voyageai
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams

anthropic_client = anthropic.Anthropic()
voyage_client = voyageai.Client()
qdrant = QdrantClient(":memory:")  # Use real URL in production

HYDE_SYSTEM = """Generate a hypothetical document passage that would answer the following question.
Write 2-3 sentences as if you are the document, not the answer. Use domain-appropriate vocabulary.
Do NOT say you are generating a hypothetical answer — just write the passage directly."""

def generate_hypothetical_document(question: str) -> str:
    """Use LLM to generate a hypothetical answer passage."""
    response = anthropic_client.messages.create(
        model="claude-haiku-4-5",   # Use fast/cheap model for HyDE generation
        max_tokens=256,
        temperature=0.3,
        system=HYDE_SYSTEM,
        messages=[{"role": "user", "content": question}]
    )
    return response.content[0].text

def hyde_retrieve(question: str, collection: str, top_k: int = 5) -> list[dict]:
    """Retrieve using HyDE: embed hypothetical answer instead of question."""
    # Generate hypothetical document
    hypothetical = generate_hypothetical_document(question)
    print(f"Hypothetical: {hypothetical[:120]}...")

    # Embed the hypothetical document (not the question)
    result = voyage_client.embed([hypothetical], model="voyage-3")
    hyde_vector = result.embeddings[0]

    # Search with hypothetical embedding
    hits = qdrant.search(
        collection_name=collection,
        query_vector=hyde_vector,
        limit=top_k,
    )
    return [{"content": h.payload["content"], "score": h.score} for h in hits]

def standard_retrieve(question: str, collection: str, top_k: int = 5) -> list[dict]:
    """Standard retrieval: embed question directly."""
    result = voyage_client.embed([question], model="voyage-3")
    query_vector = result.embeddings[0]
    hits = qdrant.search(
        collection_name=collection,
        query_vector=query_vector,
        limit=top_k,
    )
    return [{"content": h.payload["content"], "score": h.score} for h in hits]

# Usage comparison
question = "What are the side effects of combining ACE inhibitors with potassium supplements?"
hyde_results = hyde_retrieve(question, "medical-docs")
standard_results = standard_retrieve(question, "medical-docs")`}}),e.jsx("h2",{children:"HyDE with Multiple Hypothetical Documents"}),e.jsx("p",{children:'A single hypothetical document may not capture all relevant angles of a complex question. Generate 3–5 hypothetical documents with slight temperature variation, embed all of them, and either average the vectors or retrieve independently and merge the result sets. Averaging the embeddings (sometimes called "centroid HyDE") produces a more stable search vector that is less sensitive to the specific phrasing of any single generation.'}),e.jsx(t,{title:"Multi-Hypothetical HyDE with Vector Averaging",tabs:{python:`import numpy as np
import anthropic
import voyageai

anthropic_client = anthropic.Anthropic()
voyage_client = voyageai.Client()

def multi_hyde_retrieve(question: str, num_hypotheticals: int = 3) -> np.ndarray:
    """Generate multiple hypotheticals and average their embeddings."""
    hypotheticals = []
    for i in range(num_hypotheticals):
        response = anthropic_client.messages.create(
            model="claude-haiku-4-5",
            max_tokens=200,
            temperature=0.5,   # Higher temp for diverse hypotheticals
            system="Write a passage from a document that would answer this question.",
            messages=[{"role": "user", "content": question}]
        )
        hypotheticals.append(response.content[0].text)

    # Embed all hypotheticals in a single batch call
    result = voyage_client.embed(hypotheticals, model="voyage-3")
    vectors = np.array(result.embeddings)

    # Average the vectors to get a centroid
    centroid = vectors.mean(axis=0)

    # Normalize to unit length (important for cosine similarity)
    centroid = centroid / np.linalg.norm(centroid)
    return centroid

# The centroid is more robust than any single hypothetical vector
centroid_vector = multi_hyde_retrieve(
    "How does transformer attention scale with sequence length?"
)
print(f"Centroid vector shape: {centroid_vector.shape}")`}}),e.jsx(n,{title:"Use a Fast, Cheap Model for HyDE Generation",children:e.jsx("p",{children:"HyDE adds an extra LLM call to every query. Use your fastest, cheapest model for this step — a small model like claude-haiku or GPT-4o-mini generates hypothetical documents of sufficient quality for most domains. Reserve your stronger model for the final answer generation step. This keeps the latency and cost overhead of HyDE manageable."})}),e.jsx(s,{title:"Always A/B test HyDE against direct retrieval",content:"HyDE is not universally better. Measure retrieval recall@5 with both approaches on your golden dataset. Common finding: HyDE helps on technical or scientific corpora where question phrasing diverges from document phrasing. It often does not help — and can hurt — on conversational or short-document corpora where questions and documents use similar vocabulary. Treat it as one tool to evaluate, not a default upgrade."})]})}const E=Object.freeze(Object.defineProperty({__proto__:null,default:a},Symbol.toStringTag,{value:"Module"}));function l(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Query Rewriting"}),e.jsx("p",{children:`Users rarely phrase their questions the way documents are written. A question like "how do I fix the login bug?" maps poorly to a document titled "Authentication Error Resolution Guide." Query rewriting transforms the user's original question into one or more search queries that are better aligned with the vocabulary and structure of the document corpus. It is one of the highest-leverage improvements available for a mature RAG system.`}),e.jsx(r,{title:"Query Rewriting",definition:"Query rewriting uses a language model to transform or expand the user's original query before retrieval. The rewritten query — or set of queries — is better aligned with how information is phrased in the knowledge base, covering synonyms, related terms, alternative phrasings, and decomposed sub-questions that together address the user's intent."}),e.jsx("h2",{children:"Multi-Query Retrieval"}),e.jsx("p",{children:"Instead of searching with a single query, generate 3–5 alternative phrasings of the same question and retrieve for all of them. Merge the result sets and deduplicate. This broadens coverage and reduces the chance that an unusual phrasing causes a relevant document to be missed."}),e.jsx(t,{title:"Multi-Query Retrieval",tabs:{python:`import anthropic
from langchain_community.vectorstores import Chroma
from langchain_voyageai import VoyageAIEmbeddings

client = anthropic.Anthropic()

MULTI_QUERY_PROMPT = """Generate {n} different search queries that would retrieve documents
relevant to answering this question. Each query should take a different angle.
Return ONLY the queries, one per line, no numbering or explanation.

Question: {question}"""

def generate_multiple_queries(question: str, n: int = 4) -> list[str]:
    response = client.messages.create(
        model="claude-haiku-4-5",
        max_tokens=256,
        temperature=0.4,
        messages=[{
            "role": "user",
            "content": MULTI_QUERY_PROMPT.format(n=n, question=question)
        }]
    )
    queries = [q.strip() for q in response.content[0].text.strip().split("\\n") if q.strip()]
    return queries[:n]  # Safety cap

def multi_query_retrieve(question: str, vectorstore, top_k: int = 3) -> list[str]:
    """Retrieve using multiple query variants, deduplicated."""
    queries = generate_multiple_queries(question)
    queries.append(question)  # Always include the original

    seen_ids = set()
    all_chunks = []

    for query in queries:
        results = vectorstore.similarity_search(query, k=top_k)
        for doc in results:
            doc_id = doc.metadata.get("id") or doc.page_content[:64]
            if doc_id not in seen_ids:
                seen_ids.add(doc_id)
                all_chunks.append(doc.page_content)

    return all_chunks

# Example
embeddings = VoyageAIEmbeddings(model="voyage-3")
# vectorstore = Chroma(embedding_function=embeddings, persist_directory="./chroma_db")

question = "What are the performance implications of using async in Python?"
queries = generate_multiple_queries(question)
for i, q in enumerate(queries, 1):
    print(f"{i}. {q}")`}}),e.jsx("h2",{children:"Step-Back Prompting"}),e.jsx("p",{children:"Step-back prompting asks the model to identify the underlying principle or more general concept behind a specific question. Instead of searching for the specific case, you first retrieve general background knowledge, then use that alongside any specific results to generate the answer. This is particularly effective for reasoning questions where context about the broader domain helps."}),e.jsx(t,{title:"Step-Back Prompting for Query Expansion",tabs:{python:`import anthropic

client = anthropic.Anthropic()

STEP_BACK_PROMPT = """Given this specific question, generate a more general "step-back" question
that retrieves background knowledge needed to answer the original question.

Original: {question}
Step-back question (more general concept/principle):"""

def step_back_query(question: str) -> tuple[str, str]:
    """Return (original_question, step_back_question) pair."""
    response = client.messages.create(
        model="claude-haiku-4-5",
        max_tokens=128,
        temperature=0.2,
        messages=[{"role": "user", "content": STEP_BACK_PROMPT.format(question=question)}]
    )
    step_back = response.content[0].text.strip()
    return question, step_back

def step_back_retrieve(question: str, vectorstore) -> list[str]:
    """Retrieve with both the specific question and its step-back generalisation."""
    original, generalised = step_back_query(question)

    specific_docs = vectorstore.similarity_search(original, k=3)
    general_docs = vectorstore.similarity_search(generalised, k=2)

    # Deduplicate and combine
    seen = set()
    combined = []
    for doc in specific_docs + general_docs:
        key = doc.page_content[:64]
        if key not in seen:
            seen.add(key)
            combined.append(doc.page_content)

    return combined

# Example step-backs
examples = [
    "Why does my transformer model overfit with a small dataset?",
    "How do I tune the ef parameter in HNSW for my use case?",
    "Why is cosine similarity returning 0.3 for clearly related sentences?",
]
for q in examples:
    original, step_back = step_back_query(q)
    print(f"Original:  {original}")
    print(f"Step-back: {step_back}")
    print()`}}),e.jsx("h2",{children:"Query Decomposition"}),e.jsx("p",{children:"Complex questions that require multiple pieces of information should be decomposed into atomic sub-questions, each retrievable independently. The sub-question answers are then synthesised into the final answer. This is the basis of the sub-question query engine in LlamaIndex."}),e.jsx(t,{title:"Query Decomposition",tabs:{python:`import anthropic
import json

client = anthropic.Anthropic()

DECOMPOSE_PROMPT = """Decompose this complex question into 2-4 simpler sub-questions
that can each be answered independently from a document corpus.
Return a JSON array of sub-question strings.

Question: {question}
Sub-questions (JSON array):"""

def decompose_query(question: str) -> list[str]:
    response = client.messages.create(
        model="claude-haiku-4-5",
        max_tokens=256,
        temperature=0.1,
        messages=[{"role": "user", "content": DECOMPOSE_PROMPT.format(question=question)}]
    )
    raw = response.content[0].text.strip()
    try:
        sub_questions = json.loads(raw)
        return sub_questions if isinstance(sub_questions, list) else [question]
    except json.JSONDecodeError:
        return [question]  # Fallback to original

def decomposed_rag(question: str, vectorstore, llm_client) -> str:
    """Answer a complex question by decomposing it and retrieving for each sub-question."""
    sub_questions = decompose_query(question)
    print(f"Decomposed into {len(sub_questions)} sub-questions:")
    for i, q in enumerate(sub_questions, 1):
        print(f"  {i}. {q}")

    # Retrieve for each sub-question
    all_context = []
    for sq in sub_questions:
        docs = vectorstore.similarity_search(sq, k=2)
        all_context.extend([d.page_content for d in docs])

    # Deduplicate context
    seen = set()
    unique_context = []
    for chunk in all_context:
        key = chunk[:64]
        if key not in seen:
            seen.add(key)
            unique_context.append(chunk)

    # Generate final answer from combined context
    context_str = "\\n\\n---\\n\\n".join(unique_context)
    response = llm_client.messages.create(
        model="claude-opus-4-5",
        max_tokens=1024,
        temperature=0.1,
        system="Answer the question using the provided context. Synthesise information from all relevant parts.",
        messages=[{"role": "user", "content": f"Context:\\n{context_str}\\n\\nQuestion: {question}"}]
    )
    return response.content[0].text

# Test decomposition
q = "How does HNSW differ from IVF indexing in terms of memory usage and query performance?"
sub_qs = decompose_query(q)
print(sub_qs)`}}),e.jsx(n,{title:"Profile Before Optimising Query Rewriting",children:e.jsx("p",{children:"Query rewriting adds latency (an extra LLM call) and cost. Before implementing it, profile where your RAG pipeline fails: if low retrieval recall is the bottleneck, multi-query or step-back rewriting may help. If retrieval is already high quality and the problem is in generation, rewriting will not help. Use your golden dataset's retrieval recall metric to make this decision empirically."})}),e.jsx(s,{title:"Caching rewritten queries",content:"For production systems, cache rewritten queries by the hash of the original question. Query rewriting is deterministic at low temperature — the same question will produce the same rewrites. Caching eliminates the extra LLM call for repeated questions, reducing both latency and cost. A simple in-memory dict or Redis TTL cache is sufficient for most deployments."})]})}const L=Object.freeze(Object.defineProperty({__proto__:null,default:l},Symbol.toStringTag,{value:"Module"}));function c(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"RAG Fusion"}),e.jsx("p",{children:"RAG Fusion extends multi-query retrieval with a principled algorithm for combining ranked result lists from multiple queries: Reciprocal Rank Fusion (RRF). Rather than simply concatenating and deduplicating results, RRF assigns a score to each document based on its rank in each individual result list, then sorts all documents by their fused score. Documents that consistently appear near the top across multiple queries receive the highest fused rank."}),e.jsx(r,{title:"Reciprocal Rank Fusion (RRF)",definition:"RRF is a rank aggregation algorithm that combines multiple ranked lists into a single fused ranking. Each document receives a score of 1/(k + rank) for each list it appears in, where k is a smoothing constant (typically 60). Final scores are summed across all lists. Documents that appear consistently at the top across multiple ranked lists accumulate high scores, making RRF robust to any single retrieval producing a poor ranking."}),e.jsx("h2",{children:"RAG Fusion Flow"}),e.jsx("p",{children:"The RAG Fusion pipeline has four stages: (1) generate multiple query variants from the original question, (2) retrieve the top-k documents for each query independently, (3) apply RRF to fuse the ranked lists into a single ranking, (4) pass the top-n fused results as context to the generator."}),e.jsx(t,{title:"RRF Algorithm Implementation",tabs:{python:`from collections import defaultdict
from typing import Any

def reciprocal_rank_fusion(
    ranked_lists: list[list[tuple[str, Any]]],  # List of (doc_id, doc_content) lists
    k: int = 60,
) -> list[tuple[str, float, Any]]:
    """
    Fuse multiple ranked document lists using Reciprocal Rank Fusion.

    Args:
        ranked_lists: Each inner list is a ranked result set [(doc_id, content), ...]
                      ordered from most to least relevant.
        k: Smoothing constant. Higher k reduces the impact of top ranks.
           k=60 is the canonical value from the original RRF paper.

    Returns:
        List of (doc_id, rrf_score, content) sorted by descending RRF score.
    """
    scores: dict[str, float] = defaultdict(float)
    content_map: dict[str, Any] = {}

    for ranked_list in ranked_lists:
        for rank, (doc_id, content) in enumerate(ranked_list, start=1):
            scores[doc_id] += 1.0 / (k + rank)
            content_map[doc_id] = content  # Last write wins (content is same)

    fused = [
        (doc_id, score, content_map[doc_id])
        for doc_id, score in sorted(scores.items(), key=lambda x: x[1], reverse=True)
    ]
    return fused


# Example: three retrieval runs returning different orderings
list_a = [("doc1", "content1"), ("doc3", "content3"), ("doc2", "content2")]
list_b = [("doc2", "content2"), ("doc1", "content1"), ("doc5", "content5")]
list_c = [("doc1", "content1"), ("doc4", "content4"), ("doc2", "content2")]

fused = reciprocal_rank_fusion([list_a, list_b, list_c])
for doc_id, score, _ in fused:
    print(f"{doc_id}: RRF score = {score:.4f}")
# doc1 appears in all three lists at top positions → highest score
# doc2 appears in all three but lower → second
# doc3, doc4, doc5 appear once → lower scores`}}),e.jsx("h2",{children:"Full RAG Fusion Pipeline"}),e.jsx(t,{title:"Complete RAG Fusion Pipeline",tabs:{python:`import anthropic
import voyageai
from qdrant_client import QdrantClient
from collections import defaultdict

anthropic_client = anthropic.Anthropic()
voyage_client = voyageai.Client()
qdrant = QdrantClient(url="http://localhost:6333")

QUERY_GEN_PROMPT = """Generate {n} search queries to retrieve documents for answering:
"{question}"

Write each query on a new line. Vary phrasing, perspective, and specificity.
Include the original question as one of them."""

def generate_queries(question: str, n: int = 4) -> list[str]:
    response = anthropic_client.messages.create(
        model="claude-haiku-4-5",
        max_tokens=200,
        temperature=0.5,
        messages=[{
            "role": "user",
            "content": QUERY_GEN_PROMPT.format(n=n, question=question)
        }]
    )
    queries = [q.strip() for q in response.content[0].text.strip().split("\\n") if q.strip()]
    return queries[:n]

def retrieve_for_query(query: str, collection: str, top_k: int = 10) -> list[tuple[str, dict]]:
    """Retrieve top_k documents for a single query. Returns (doc_id, payload) pairs."""
    result = voyage_client.embed([query], model="voyage-3")
    hits = qdrant.search(
        collection_name=collection,
        query_vector=result.embeddings[0],
        limit=top_k,
        with_payload=True,
    )
    return [(hit.id, hit.payload) for hit in hits]

def rag_fusion(
    question: str,
    collection: str,
    num_queries: int = 4,
    top_k_per_query: int = 10,
    final_top_n: int = 5,
    rrf_k: int = 60,
) -> list[dict]:
    """Full RAG Fusion: multi-query retrieval + RRF reranking."""
    queries = generate_queries(question, n=num_queries)

    # Retrieve ranked lists for each query
    ranked_lists = []
    for q in queries:
        ranked_list = retrieve_for_query(q, collection, top_k=top_k_per_query)
        ranked_lists.append(ranked_list)

    # Apply RRF
    scores: dict[str, float] = defaultdict(float)
    payloads: dict[str, dict] = {}
    for ranked_list in ranked_lists:
        for rank, (doc_id, payload) in enumerate(ranked_list, start=1):
            scores[doc_id] += 1.0 / (rrf_k + rank)
            payloads[doc_id] = payload

    # Sort by fused score and return top N
    sorted_docs = sorted(scores.items(), key=lambda x: x[1], reverse=True)
    return [
        {"id": doc_id, "rrf_score": score, "content": payloads[doc_id].get("content", "")}
        for doc_id, score in sorted_docs[:final_top_n]
    ]

# Usage
results = rag_fusion(
    "How do I configure connection pooling in PostgreSQL for a high-traffic application?",
    collection="tech-docs",
)
for r in results:
    print(f"Score: {r['rrf_score']:.4f} | {r['content'][:80]}...")`}}),e.jsx("h2",{children:"Tuning the RRF k Parameter"}),e.jsxs("p",{children:["The constant ",e.jsx("code",{children:"k=60"})," comes from the original 2009 RRF paper and works well in most settings. Lower values of k increase the relative weight of top-ranked results — a document ranked 1st receives a much higher score than one ranked 2nd. Higher values flatten the distribution, giving more equal weight across ranks. For RAG with high-quality vector retrieval, k=60 is a good default. For sparse retrieval or BM25 where rank 1 is not always reliable, slightly higher k (80–100) may give better results."]}),e.jsx(n,{title:"RAG Fusion Adds Latency — Parallelize Retrieval",children:e.jsxs("p",{children:["RAG Fusion runs multiple retrieval queries. If done sequentially, this multiplies your retrieval latency by the number of queries. Use ",e.jsx("code",{children:"asyncio.gather"})," or a thread pool to fire all retrieval requests in parallel. Since vector database queries are I/O-bound, parallel execution brings the total retrieval latency close to a single query's latency, regardless of how many queries you generate."]})}),e.jsx(s,{title:"RRF vs. score-based fusion",content:"RRF uses rank positions rather than raw similarity scores. This makes it robust to score distribution differences between retrieval runs — you don't need to calibrate or normalise scores. It also degrades gracefully when one retrieval system is lower quality: a document must rank well across multiple lists to score highly, so one bad retrieval run does not dominate the final ranking."})]})}const M=Object.freeze(Object.defineProperty({__proto__:null,default:c},Symbol.toStringTag,{value:"Module"}));function d(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Hybrid Search"}),e.jsx("p",{children:"Dense vector search excels at semantic similarity — it finds documents that mean the same thing even when they use different words. Sparse keyword search (BM25) excels at exact matches — it reliably finds documents containing specific product names, error codes, technical terms, or acronyms that embedding models may not handle well. Hybrid search combines both signals to capture the benefits of each."}),e.jsx(r,{title:"Hybrid Search",definition:"Hybrid search combines dense vector retrieval (embedding-based semantic search) with sparse keyword retrieval (BM25 or TF-IDF) to retrieve documents. Results from both systems are merged using a rank fusion algorithm — typically Reciprocal Rank Fusion — to produce a final ranked list that captures both semantic relevance and exact keyword matches."}),e.jsx("h2",{children:"Why Hybrid Outperforms Either Alone"}),e.jsx("p",{children:'Dense-only retrieval misses: exact product codes (SKU-7829), specific error messages ("ECONNREFUSED"), proper nouns that are rare in training data, and technical abbreviations. Sparse-only retrieval misses: paraphrases, synonyms, and conceptually related content that uses different vocabulary. Hybrid catches both classes of queries.'}),e.jsx("p",{children:"Empirically, hybrid search consistently improves retrieval recall@5 by 5–15% over either method alone across diverse corpora. The improvement is largest for corpora that mix natural language prose with technical identifiers — a common pattern in enterprise knowledge bases."}),e.jsx(t,{title:"Hybrid Search with BM25 + Dense Vectors",tabs:{python:`from rank_bm25 import BM25Okapi
import voyageai
import numpy as np
from collections import defaultdict

voyage_client = voyageai.Client()

class HybridRetriever:
    def __init__(self, documents: list[dict]):
        """
        documents: list of {"id": str, "content": str}
        """
        self.documents = documents
        self.doc_ids = [d["id"] for d in documents]
        self.contents = [d["content"] for d in documents]

        # Build BM25 index
        tokenized = [doc.lower().split() for doc in self.contents]
        self.bm25 = BM25Okapi(tokenized)

        # Build dense index (embed all documents)
        result = voyage_client.embed(self.contents, model="voyage-3")
        self.dense_vectors = np.array(result.embeddings)

    def dense_search(self, query: str, top_k: int = 20) -> list[tuple[str, int]]:
        """Returns (doc_id, rank) pairs sorted by cosine similarity."""
        result = voyage_client.embed([query], model="voyage-3")
        query_vec = np.array(result.embeddings[0])

        # Cosine similarity (vectors assumed normalised)
        scores = self.dense_vectors @ query_vec
        ranked_indices = np.argsort(scores)[::-1][:top_k]
        return [(self.doc_ids[i], rank + 1) for rank, i in enumerate(ranked_indices)]

    def sparse_search(self, query: str, top_k: int = 20) -> list[tuple[str, int]]:
        """Returns (doc_id, rank) pairs sorted by BM25 score."""
        tokenized_query = query.lower().split()
        scores = self.bm25.get_scores(tokenized_query)
        ranked_indices = np.argsort(scores)[::-1][:top_k]
        return [(self.doc_ids[i], rank + 1) for rank, i in enumerate(ranked_indices)]

    def hybrid_search(
        self,
        query: str,
        top_k: int = 5,
        rrf_k: int = 60,
        dense_weight: float = 0.7,
        sparse_weight: float = 0.3,
    ) -> list[dict]:
        """Combine dense and sparse with weighted RRF."""
        dense_results = self.dense_search(query, top_k=20)
        sparse_results = self.sparse_search(query, top_k=20)

        scores: dict[str, float] = defaultdict(float)
        for doc_id, rank in dense_results:
            scores[doc_id] += dense_weight * (1.0 / (rrf_k + rank))
        for doc_id, rank in sparse_results:
            scores[doc_id] += sparse_weight * (1.0 / (rrf_k + rank))

        sorted_ids = sorted(scores.items(), key=lambda x: x[1], reverse=True)
        id_to_content = {d["id"]: d["content"] for d in self.documents}

        return [
            {"id": doc_id, "score": score, "content": id_to_content[doc_id]}
            for doc_id, score in sorted_ids[:top_k]
            if doc_id in id_to_content
        ]

# Usage
docs = [
    {"id": "d1", "content": "PostgreSQL connection pooling with PgBouncer improves throughput."},
    {"id": "d2", "content": "Database performance tuning for high-traffic web applications."},
    {"id": "d3", "content": "Error ECONNREFUSED when connecting to PostgreSQL on port 5432."},
]
retriever = HybridRetriever(docs)
results = retriever.hybrid_search("ECONNREFUSED postgres connection error")
for r in results:
    print(f"{r['id']} ({r['score']:.4f}): {r['content'][:60]}")`}}),e.jsx("h2",{children:"Hybrid Search with Weaviate"}),e.jsxs("p",{children:["Weaviate supports hybrid search natively, combining BM25 and vector search with a single API call. The ",e.jsx("code",{children:"alpha"})," parameter controls the blend: 0 = pure BM25, 1 = pure vector, 0.5 = equal weight. This is the simplest path to production hybrid search if you are already using Weaviate."]}),e.jsx(t,{title:"Weaviate Native Hybrid Search",tabs:{python:`import weaviate
import weaviate.classes as wvc

client = weaviate.connect_to_local()  # or connect_to_weaviate_cloud(...)

# Create collection with hybrid search support
if not client.collections.exists("Documents"):
    client.collections.create(
        name="Documents",
        vectorizer_config=wvc.config.Configure.Vectorizer.text2vec_voyageai(
            model="voyage-3",
        ),
        properties=[
            wvc.config.Property(name="content", data_type=wvc.config.DataType.TEXT),
            wvc.config.Property(name="source", data_type=wvc.config.DataType.TEXT),
        ]
    )

collection = client.collections.get("Documents")

def hybrid_search_weaviate(
    query: str,
    alpha: float = 0.75,  # 0=BM25 only, 1=vector only
    limit: int = 5,
) -> list[dict]:
    """
    alpha=0.75 gives 75% weight to vector search, 25% to BM25.
    Good default for most RAG applications.
    """
    results = collection.query.hybrid(
        query=query,
        alpha=alpha,
        limit=limit,
        return_metadata=wvc.query.MetadataQuery(score=True, explain_score=True),
    )
    return [
        {
            "content": obj.properties["content"],
            "source": obj.properties["source"],
            "score": obj.metadata.score,
        }
        for obj in results.objects
    ]

# Query with mixed semantic + keyword needs
results = hybrid_search_weaviate(
    "authentication error ECONNREFUSED service account",
    alpha=0.6,  # Slightly more weight to keyword match for this error-code query
)
for r in results:
    print(f"Score: {r['score']:.4f} | {r['content'][:80]}")

client.close()`}}),e.jsx(n,{title:"Tune the Dense/Sparse Balance for Your Query Distribution",children:e.jsx("p",{children:"The optimal alpha (dense/sparse blend) depends on your query distribution. Corpora with many exact-match queries (error codes, product IDs, names) benefit from higher sparse weight (alpha 0.5–0.6). Corpora dominated by natural language conceptual questions benefit from higher dense weight (alpha 0.7–0.8). Evaluate both extremes and the midpoint on your golden dataset's retrieval recall to find the sweet spot."})}),e.jsx(s,{title:"BM25 requires tokenisation consistency",content:"BM25 is sensitive to tokenisation: if you tokenise documents differently at index time vs. query time (e.g., lowercasing, stemming, stopword removal), recall will drop. Use the same tokeniser for both. Most production hybrid search implementations handle this automatically, but when building BM25 from scratch (e.g., rank-bm25 library), ensure your query tokenisation pipeline exactly matches the indexing pipeline."})]})}const P=Object.freeze(Object.defineProperty({__proto__:null,default:d},Symbol.toStringTag,{value:"Module"}));function u(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Reranking"}),e.jsx("p",{children:"Vector similarity search is fast but approximate — it optimises for speed using Approximate Nearest Neighbor algorithms, and the query embedding captures only a compressed representation of meaning. Reranking is a second pass over the initial retrieval results that uses a more powerful but slower model to re-score each candidate document against the query, then re-sorts them. This two-stage approach combines the scalability of vector search with the accuracy of cross-encoders."}),e.jsx(r,{title:"Cross-Encoder Reranking",definition:"A cross-encoder is a model that takes a (query, document) pair as joint input and outputs a relevance score. Unlike bi-encoders (embedding models) which produce independent query and document vectors, cross-encoders attend to both texts simultaneously, enabling much finer-grained relevance assessment. They are too slow to run over an entire corpus but fast enough to re-score the 20–100 candidates returned by initial retrieval."}),e.jsx("h2",{children:"The Two-Stage Retrieval Pattern"}),e.jsx("p",{children:"Stage 1: retrieve a large candidate set (top 20–100) using fast vector search. Stage 2: re-score each candidate with a cross-encoder reranker and return the top 5–10 reranked results. The first stage prioritises recall (cast a wide net); the second stage prioritises precision (pick the best from the net). This pattern consistently outperforms either stage alone."}),e.jsx(t,{title:"Cohere Rerank API",tabs:{python:`import cohere
import voyageai
from qdrant_client import QdrantClient

voyage_client = voyageai.Client()
cohere_client = cohere.Client()
qdrant = QdrantClient(url="http://localhost:6333")

def two_stage_retrieve(
    query: str,
    collection: str,
    initial_k: int = 30,    # Wide first-stage retrieval
    final_k: int = 5,       # Narrow final output after reranking
) -> list[dict]:
    """Two-stage retrieval: vector search + Cohere reranking."""

    # Stage 1: Vector retrieval (fast, recall-oriented)
    result = voyage_client.embed([query], model="voyage-3")
    hits = qdrant.search(
        collection_name=collection,
        query_vector=result.embeddings[0],
        limit=initial_k,
        with_payload=True,
    )
    candidates = [
        {"id": h.id, "content": h.payload.get("content", ""), "vector_score": h.score}
        for h in hits
    ]

    if not candidates:
        return []

    # Stage 2: Cohere reranking (slower, precision-oriented)
    rerank_response = cohere_client.rerank(
        query=query,
        documents=[c["content"] for c in candidates],
        model="rerank-english-v3.0",
        top_n=final_k,
    )

    # Map reranked indices back to candidates
    reranked = []
    for result in rerank_response.results:
        candidate = candidates[result.index]
        reranked.append({
            **candidate,
            "rerank_score": result.relevance_score,
        })

    return reranked

# Usage
results = two_stage_retrieve(
    "How do I configure connection timeout for gRPC clients?",
    collection="tech-docs",
)
for r in results:
    print(f"Rerank: {r['rerank_score']:.3f} | Vector: {r['vector_score']:.3f} | {r['content'][:60]}")`}}),e.jsx("h2",{children:"Local Cross-Encoder with sentence-transformers"}),e.jsxs("p",{children:["For privacy-sensitive deployments or to avoid reranking API costs, run a cross-encoder locally. The ",e.jsx("code",{children:"sentence-transformers"})," library provides several pre-trained cross-encoders optimised for relevance scoring."]}),e.jsx(t,{title:"Local Cross-Encoder Reranking",tabs:{python:`from sentence_transformers import CrossEncoder
import numpy as np

# Load once at startup — keep in memory
reranker = CrossEncoder("cross-encoder/ms-marco-MiniLM-L-6-v2")
# Alternatives:
# "cross-encoder/ms-marco-MiniLM-L-12-v2"   — more accurate, slower
# "BAAI/bge-reranker-v2-m3"                 — multilingual, state-of-the-art

def local_rerank(
    query: str,
    candidates: list[dict],  # Each must have "content" key
    top_k: int = 5,
) -> list[dict]:
    """Rerank candidates using a local cross-encoder."""
    if not candidates:
        return []

    # Cross-encoder expects (query, document) pairs
    pairs = [(query, c["content"]) for c in candidates]
    scores = reranker.predict(pairs)

    # Attach scores and sort
    for candidate, score in zip(candidates, scores):
        candidate["rerank_score"] = float(score)

    return sorted(candidates, key=lambda x: x["rerank_score"], reverse=True)[:top_k]

# Benchmark reranking latency
import time

query = "What is the capital of France?"
candidates = [
    {"content": "Paris is the capital and largest city of France."},
    {"content": "France is a country in Western Europe."},
    {"content": "The Eiffel Tower is located in Paris, France."},
    {"content": "Lyon is the third-largest city in France."},
    {"content": "France's president resides in the Élysée Palace in Paris."},
]

start = time.time()
results = local_rerank(query, candidates, top_k=3)
elapsed = (time.time() - start) * 1000

print(f"Reranking {len(candidates)} candidates took {elapsed:.1f}ms")
for r in results:
    print(f"Score: {r['rerank_score']:.3f} | {r['content'][:60]}")`}}),e.jsx("h2",{children:"When to Use Reranking"}),e.jsx("p",{children:"Reranking is most valuable when: (1) your initial retrieval returns many marginally relevant results and you need to identify the truly relevant ones; (2) you are dealing with queries that have multiple plausible interpretations and initial retrieval misses the intended meaning; (3) your documents have variable length and shorter, highly relevant chunks are being outscored by longer, partially relevant ones."}),e.jsx("p",{children:"Reranking is less valuable when: your initial retrieval already achieves high precision at top-5, your queries are short and unambiguous, or latency is a hard constraint and you cannot afford the extra 50–200ms."}),e.jsx(n,{title:"Measure Reranking Latency in Your Environment",children:e.jsx("p",{children:"Reranking latency depends on model size, number of candidates, and document length. A cross-encoder on 20 candidates of 256 tokens each typically adds 50–150ms on CPU, 20–40ms on GPU. The Cohere Rerank API adds a network round-trip (typically 100–300ms). Measure in your actual deployment environment and decide whether the precision gain justifies the latency cost for your use case. For async or batch RAG pipelines, the latency cost is often irrelevant."})}),e.jsx(i,{title:"Reranking Does Not Fix Bad Retrieval",children:e.jsx("p",{children:"Reranking can only re-order candidates already in the initial retrieval set. If the relevant document is not in your top-k initial results, no reranker can recover it. Set your initial retrieval k high enough (typically 20–50) to ensure the relevant document has a good chance of being included before reranking. If retrieval recall at k=50 is below 80%, fix your retrieval before adding a reranker."})}),e.jsx(s,{title:"ColBERT: an alternative to cross-encoders",content:"ColBERT (Contextualised Late Interaction over BERT) uses token-level embeddings rather than sentence-level vectors, enabling interaction between every query token and every document token at retrieval time. It is more accurate than bi-encoders and faster than full cross-encoders. The RAGatouille library provides a simple Python interface to ColBERT and is worth evaluating if you need high-accuracy retrieval and can afford a few hundred milliseconds of latency."})]})}const O=Object.freeze(Object.defineProperty({__proto__:null,default:u},Symbol.toStringTag,{value:"Module"}));function h(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Contextual Compression"}),e.jsx("p",{children:"Retrieved chunks often contain more text than what is relevant to the user's question. A 512-token chunk retrieved for a narrow factual query may have 450 tokens of tangential context and only 60 tokens that directly address the question. Contextual compression extracts just the relevant portions from each retrieved chunk before passing them to the generator, reducing noise and saving context window space."}),e.jsx(r,{title:"Contextual Compression",definition:"Contextual compression is a post-retrieval technique that filters or extracts the relevant portions of retrieved chunks with respect to the user's query. Instead of sending entire chunks as context, it identifies and extracts only the sentences or passages within each chunk that are directly relevant, reducing context length and improving signal-to-noise ratio."}),e.jsx("h2",{children:"Why Compression Matters"}),e.jsx("p",{children:'The "lost-in-the-middle" problem shows that LLMs are less reliable at using information placed in the middle of long context windows. Shorter, more focused context is better processed. Compression also reduces token costs and allows more retrieved chunks to fit within a fixed context budget — if each raw chunk is 512 tokens and you compress to 100 tokens per chunk, you can fit 5× more unique sources in the same context window.'}),e.jsx(t,{title:"LLM-Based Contextual Compression",tabs:{python:`import anthropic

client = anthropic.Anthropic()

EXTRACT_PROMPT = """Given the following document passage and a question, extract ONLY the
sentences from the passage that are directly relevant to answering the question.
If no sentences are relevant, respond with exactly: "NO_RELEVANT_CONTENT"
Do not add any explanation — return only the extracted sentences.

Question: {question}

Passage:
{passage}

Relevant sentences:"""

def compress_chunk(question: str, chunk: str, model: str = "claude-haiku-4-5") -> str | None:
    """
    Extract relevant sentences from a chunk.
    Returns None if no relevant content found.
    """
    response = client.messages.create(
        model=model,
        max_tokens=256,
        temperature=0.0,
        messages=[{
            "role": "user",
            "content": EXTRACT_PROMPT.format(question=question, passage=chunk)
        }]
    )
    result = response.content[0].text.strip()
    if result == "NO_RELEVANT_CONTENT" or not result:
        return None
    return result

def compress_chunks(question: str, chunks: list[str], min_length: int = 20) -> list[str]:
    """
    Compress a list of retrieved chunks, filtering out irrelevant ones.
    Returns only the relevant extracted portions.
    """
    compressed = []
    for chunk in chunks:
        extracted = compress_chunk(question, chunk)
        if extracted and len(extracted) >= min_length:
            compressed.append(extracted)
    return compressed

# Example
question = "What is the maximum context window of Claude claude-opus-4-5?"

chunks = [
    """Anthropic offers several Claude models with varying capabilities.
    Claude claude-opus-4-5 features a 200,000 token context window, making it suitable
    for processing long documents. The model was released in 2024 and supports
    function calling, vision, and streaming. Anthropic's API pricing is token-based.""",

    """Python is a high-level programming language known for its readability.
    It supports multiple programming paradigms including procedural, object-oriented,
    and functional styles. Python is widely used in data science and machine learning.""",
]

compressed = compress_chunks(question, chunks)
print(f"Original: {sum(len(c) for c in chunks)} chars")
print(f"Compressed: {sum(len(c) for c in compressed)} chars")
for i, c in enumerate(compressed):
    print(f"Chunk {i+1}: {c}")`}}),e.jsx("h2",{children:"LangChain ContextualCompressionRetriever"}),e.jsxs("p",{children:["LangChain provides a built-in wrapper that combines a base retriever with a compressor, handling the pipeline mechanics automatically. The ",e.jsx("code",{children:"LLMChainExtractor"})," uses an LLM to extract relevant content; the ",e.jsx("code",{children:"EmbeddingsFilter"})," uses cosine similarity as a cheaper alternative."]}),e.jsx(t,{title:"LangChain ContextualCompressionRetriever",tabs:{python:`from langchain.retrievers import ContextualCompressionRetriever
from langchain.retrievers.document_compressors import LLMChainExtractor, EmbeddingsFilter
from langchain_anthropic import ChatAnthropic
from langchain_voyageai import VoyageAIEmbeddings
from langchain_community.vectorstores import Chroma

# Base retriever
embeddings = VoyageAIEmbeddings(model="voyage-3")
vectorstore = Chroma(embedding_function=embeddings, persist_directory="./chroma_db")
base_retriever = vectorstore.as_retriever(search_kwargs={"k": 10})

# Option 1: LLM-based extractor (most accurate, most expensive)
llm = ChatAnthropic(model="claude-haiku-4-5")
llm_extractor = LLMChainExtractor.from_llm(llm)
llm_compression_retriever = ContextualCompressionRetriever(
    base_compressor=llm_extractor,
    base_retriever=base_retriever,
)

# Option 2: Embeddings-based filter (fast, cheap, good enough for most cases)
embeddings_filter = EmbeddingsFilter(
    embeddings=embeddings,
    similarity_threshold=0.76,  # Only keep chunks with cosine sim >= 0.76 to query
)
embeddings_compression_retriever = ContextualCompressionRetriever(
    base_compressor=embeddings_filter,
    base_retriever=base_retriever,
)

# Usage — transparent drop-in for any LangChain retrieval chain
question = "What configuration options does pgvector support for HNSW indexing?"

# LLM-compressed results (extracted relevant sentences only)
compressed_docs = llm_compression_retriever.invoke(question)
print(f"LLM compression: {len(compressed_docs)} relevant passages")
for doc in compressed_docs:
    print(f"  {doc.page_content[:100]}...")

# Embeddings-filtered results (entire chunks that pass similarity threshold)
filtered_docs = embeddings_compression_retriever.invoke(question)
print(f"\\nEmbeddings filter: {len(filtered_docs)} relevant chunks")`}}),e.jsx("h2",{children:"Sentence-Level Compression"}),e.jsx("p",{children:"A lightweight alternative to LLM-based extraction is sentence-level filtering: split each chunk into sentences, embed each sentence, compute cosine similarity to the query, and keep only sentences above a threshold. This avoids an LLM call entirely and runs in milliseconds."}),e.jsx(t,{title:"Sentence-Level Embedding Filter",tabs:{python:`import voyageai
import numpy as np
import re

voyage_client = voyageai.Client()

def split_sentences(text: str) -> list[str]:
    """Simple sentence splitter."""
    sentences = re.split(r"(?<=[.!?])\\s+", text.strip())
    return [s.strip() for s in sentences if len(s.strip()) > 20]

def sentence_level_compress(
    query: str,
    chunk: str,
    threshold: float = 0.75,
    min_sentences: int = 1,
) -> str:
    """Keep only sentences that are cosine-similar to the query."""
    sentences = split_sentences(chunk)
    if not sentences:
        return chunk

    # Embed query and all sentences in one batch
    texts = [query] + sentences
    result = voyage_client.embed(texts, model="voyage-3-lite")  # Use fast model
    vectors = np.array(result.embeddings)

    query_vec = vectors[0]
    sentence_vecs = vectors[1:]

    # Cosine similarities
    norms = np.linalg.norm(sentence_vecs, axis=1, keepdims=True)
    sentence_vecs_norm = sentence_vecs / np.maximum(norms, 1e-8)
    query_norm = query_vec / (np.linalg.norm(query_vec) + 1e-8)
    similarities = sentence_vecs_norm @ query_norm

    relevant_sentences = [s for s, sim in zip(sentences, similarities) if sim >= threshold]

    if len(relevant_sentences) < min_sentences:
        # Fall back to top-scoring sentences if none pass threshold
        top_idx = np.argsort(similarities)[::-1][:min_sentences]
        relevant_sentences = [sentences[i] for i in top_idx]

    return " ".join(relevant_sentences)`}}),e.jsx(n,{title:"Use Embeddings Filter as Default, LLM Extraction for High-Stakes Queries",children:e.jsx("p",{children:"Embeddings-based filtering is 10–50× faster and cheaper than LLM-based extraction. Use it as your default compressor. Upgrade to LLM extraction for high-value queries where precision matters more than latency, or when chunks are long (over 1000 tokens) and the signal-to-noise ratio is especially low. A routing layer that checks query complexity or chunk length before choosing the compression method is a practical production pattern."})}),e.jsx(s,{title:"Compression can increase hallucination risk",content:"When a compressor extracts a sentence out of context, the extracted sentence can be misleading — it may depend on a preceding sentence for correct interpretation. Mitigate this by always including at least one sentence before and after the extracted sentences (sentence window retrieval), or by passing the full chunk alongside the extracted summary so the generator can verify."})]})}const D=Object.freeze(Object.defineProperty({__proto__:null,default:h},Symbol.toStringTag,{value:"Module"})),p=`import json
from anthropic import Anthropic
import voyageai
import httpx

client = Anthropic()
voyage = voyageai.Client()

# ---- Step 1: Retrieve and grade documents ----

def retrieve_chunks(query: str, collection, k: int = 5) -> list[dict]:
    result = voyage.embed([query], model="voyage-3")
    query_vec = result.embeddings[0]
    res = collection.query(query_embeddings=[query_vec], n_results=k)
    return [
        {"text": doc, "source": meta.get("source", ""), "score": 1 - dist}
        for doc, meta, dist in zip(
            res["documents"][0], res["metadatas"][0], res["distances"][0]
        )
    ]

def grade_document(query: str, document: str) -> str:
    """
    Grade a retrieved document for relevance.
    Returns 'relevant', 'partially_relevant', or 'irrelevant'.
    """
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=64,
        system='Grade document relevance. Return JSON: {"grade": "relevant" | "partially_relevant" | "irrelevant"}',
        messages=[{
            "role": "user",
            "content": (
                f"Question: {query}\\n\\n"
                f"Document: {document[:500]}\\n\\n"
                "Does this document contain information to answer the question?"
            )
        }],
    )
    raw = response.content[0].text.strip().strip("json").strip("")
    return json.loads(raw).get("grade", "irrelevant")

def web_search_fallback(query: str) -> list[dict]:
    """
    Fallback to web search when retrieval quality is low.
    In production, use Brave Search API, Tavily, or SerpAPI.
    """
    # Simplified stub — replace with real search API
    return [
        {
            "text": f"[Web search result for: {query}] — integrate real search API here.",
            "source": "web_search",
        }
    ]

# ---- Step 2: CRAG decision logic ----

def corrective_rag(query: str, collection) -> str:
    # 1. Retrieve initial documents
    chunks = retrieve_chunks(query, collection, k=5)

    # 2. Grade each retrieved document
    grades = []
    for chunk in chunks:
        grade = grade_document(query, chunk["text"])
        grades.append((chunk, grade))
        print(f"Grade: {grade} | {chunk['text'][:60]}...")

    # 3. Classify overall retrieval quality
    relevant = [c for c, g in grades if g == "relevant"]
    partially = [c for c, g in grades if g == "partially_relevant"]
    irrelevant_count = sum(1 for _, g in grades if g == "irrelevant")

    if len(relevant) >= 2:
        # Good retrieval: use relevant chunks directly
        action = "USE_RETRIEVED"
        final_chunks = relevant
    elif len(relevant) + len(partially) > 0 and irrelevant_count < 3:
        # Partial retrieval: use what we have + supplement with web search
        action = "USE_WITH_SUPPLEMENT"
        web_results = web_search_fallback(query)
        final_chunks = relevant + partially + web_results
    else:
        # Poor retrieval: fall back entirely to web search
        action = "FALLBACK_TO_WEB"
        final_chunks = web_search_fallback(query)

    print(f"\\nCRAG Action: {action}")

    # 4. Generate answer
    context = "\\n\\n".join(
        f"[{i+1}] Source: {c.get('source', 'unknown')}\\n{c['text']}"
        for i, c in enumerate(final_chunks[:5])
    )
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=512,
        system="Answer using only the provided context. Cite [1], [2], etc.",
        messages=[{
            "role": "user",
            "content": f"Context:\\n{context}\\n\\nQuestion: {query}"
        }],
    )
    return response.content[0].text

# Usage: corrective_rag("What is RAG?", collection)`;function m(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Corrective RAG (CRAG)"}),e.jsx("p",{children:"Standard RAG passes retrieved documents to the LLM regardless of whether those documents are actually relevant. If retrieval fails — because the query is outside the knowledge base, because embeddings mismatch the document vocabulary, or because the corpus simply does not contain the answer — the LLM receives garbage context and either hallucinates or returns a confused response. Corrective RAG addresses this by grading retrieved documents before generation and taking a corrective action when quality is low."}),e.jsx(r,{term:"Corrective RAG (CRAG)",definition:"CRAG is an iterative RAG pattern that introduces a document grading step after retrieval. Each retrieved document is assessed for relevance to the query (relevant, partially relevant, or irrelevant). Based on the overall quality of retrieved documents, CRAG takes one of three actions: use the retrieved documents as-is, supplement them with a web search, or fall back entirely to live web search when retrieval quality is poor."}),e.jsx("h2",{children:"The Three CRAG Actions"}),e.jsx("h3",{children:"CORRECT — Use Retrieved Documents"}),e.jsx("p",{children:"When the majority of retrieved documents are graded as relevant, retrieval succeeded and the standard pipeline continues. The graded documents are passed to the LLM for generation. Optionally, irrelevant documents are filtered out before generation to reduce noise."}),e.jsx("h3",{children:"AMBIGUOUS — Supplement with Web Search"}),e.jsx("p",{children:"When retrieved documents are partially relevant — some contain useful information but not enough — CRAG supplements the local retrieval with a web search. The combined context (local documents + web results) gives the LLM more complete information. This action is appropriate when the knowledge base partially covers the topic but is outdated or incomplete."}),e.jsx("h3",{children:"INCORRECT — Full Web Search Fallback"}),e.jsx("p",{children:"When retrieved documents are consistently irrelevant, local retrieval has failed entirely. CRAG falls back to live web search using a search API (Brave Search, Tavily, SerpAPI) and uses the web results as the generation context. This handles queries about recent events, niche topics not in the knowledge base, or completely off-topic queries."}),e.jsx("h2",{children:"Document Grading with an LLM"}),e.jsx("p",{children:"The grading step uses an LLM to assess each retrieved document. This is essentially a binary or three-class classification: does this document contain information relevant to answering the query? A small, fast model (Claude Haiku) is suitable for this step because the classification task is simpler than generation."}),e.jsx("p",{children:"Grading adds n LLM calls (one per retrieved document) before generation. To minimise latency, run all grading calls in parallel. With 5 retrieved chunks and parallel grading, the latency overhead is approximately one Haiku call (~100ms) rather than 5 sequential calls."}),e.jsx("h2",{children:"Knowledge Refinement"}),e.jsx("p",{children:"When documents are partially relevant, CRAG also applies a knowledge refinement step: strips irrelevant sentences from partially relevant documents before using them. This is the same extraction technique as contextual compression, applied specifically to documents that passed the grading threshold but contain noise."}),e.jsx(s,{type:"note",title:"Web search integration options",children:"For the web search fallback, Tavily is the most widely used option in RAG systems — it provides a clean JSON API designed for LLM integration, handles JavaScript-rendered pages, and returns relevant snippets rather than raw HTML. Brave Search API is a privacy-respecting alternative with good coverage. SerpAPI provides Google results. Budget for roughly $0.001–0.01 per search query depending on the provider and volume."}),e.jsx(n,{title:"Grade documents in parallel to minimise latency",children:"Grading 5 documents sequentially adds ~500ms to each query. Run all grading calls concurrently using asyncio.gather (Python) or Promise.all (TypeScript). The total latency overhead becomes ~100ms (one parallel batch) rather than ~500ms. This is the single most important optimisation for making CRAG acceptable in interactive applications."}),e.jsx("h2",{children:"Corrective RAG Implementation"}),e.jsx(t,{title:"Corrective RAG with Document Grading",tabs:{python:p}}),e.jsx("p",{children:"The implementation grades each retrieved document with Claude, classifies the overall retrieval quality into one of three action categories, and takes the appropriate corrective action before generation. The web search fallback is a stub — replace it with a real search API call in production. The decision logic is straightforward to extend: you can add more nuanced thresholds or additional actions based on your specific knowledge base characteristics and query distribution."})]})}const F=Object.freeze(Object.defineProperty({__proto__:null,default:m},Symbol.toStringTag,{value:"Module"})),g=`import json
from anthropic import Anthropic
import voyageai
import chromadb

client = Anthropic()
voyage = voyageai.Client()
chroma = chromadb.PersistentClient(path="./vector_store")

def retrieve(query: str, collection_name: str, k: int = 5) -> list[dict]:
    result = voyage.embed([query], model="voyage-3")
    collection = chroma.get_collection(collection_name)
    res = collection.query(query_embeddings=[result.embeddings[0]], n_results=k)
    return [
        {"text": doc, "source": meta.get("source", "")}
        for doc, meta in zip(res["documents"][0], res["metadatas"][0])
    ]

# ---- Self-RAG Special Tokens (simulated via prompting) ----

def decide_retrieval_needed(query: str) -> bool:
    """
    ISREL: Should we retrieve external documents for this query?
    Returns True if retrieval is needed.
    """
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=64,
        system='Decide if retrieval is needed. Return JSON: {"retrieve": true | false, "reason": "..."}',
        messages=[{
            "role": "user",
            "content": (
                f"Query: {query}\\n\\n"
                "Does this query require retrieving external documents to answer accurately? "
                "Answer false for simple factual questions, math, or conversational queries."
            )
        }],
    )
    raw = response.content[0].text.strip().strip("json").strip("")
    return json.loads(raw).get("retrieve", True)

def grade_relevance(query: str, document: str) -> bool:
    """
    ISREL: Is the retrieved document relevant to the query?
    """
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=64,
        system='Grade relevance. Return JSON: {"relevant": true | false}',
        messages=[{
            "role": "user",
            "content": f"Query: {query}\\n\\nDocument: {document[:400]}\\n\\nIs this document relevant?"
        }],
    )
    raw = response.content[0].text.strip().strip("json").strip("")
    return json.loads(raw).get("relevant", False)

def generate_with_retrieval(query: str, chunks: list[dict]) -> str:
    """Generate answer grounded in retrieved context."""
    context = "\\n\\n".join(
        f"[{i+1}] {c['text']}" for i, c in enumerate(chunks)
    )
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=512,
        system="Answer using ONLY the provided context. Cite [1], [2], etc.",
        messages=[{
            "role": "user",
            "content": f"Context:\\n{context}\\n\\nQuestion: {query}"
        }],
    )
    return response.content[0].text

def generate_without_retrieval(query: str) -> str:
    """Generate answer from parametric knowledge when retrieval is not needed."""
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=512,
        system="Answer the question directly and accurately.",
        messages=[{"role": "user", "content": query}],
    )
    return response.content[0].text

def critique_answer(query: str, answer: str, context: str) -> dict:
    """
    ISSUP + ISUSE: Is the answer supported by context? Is it useful?
    """
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=128,
        system='Critique the answer. Return JSON: {"supported": true|false, "useful": true|false, "issue": "..."}',
        messages=[{
            "role": "user",
            "content": (
                f"Question: {query}\\n\\n"
                f"Context:\\n{context[:800]}\\n\\n"
                f"Answer: {answer}\\n\\n"
                "Is this answer fully supported by the context and useful to the user?"
            )
        }],
    )
    raw = response.content[0].text.strip().strip("json").strip("")
    return json.loads(raw)

def self_rag(query: str, collection_name: str, max_retries: int = 2) -> dict:
    """
    Simulated Self-RAG pipeline using prompted special-token decisions.
    """
    # 1. Decide if retrieval is needed
    needs_retrieval = decide_retrieval_needed(query)
    print(f"Retrieval needed: {needs_retrieval}")

    if not needs_retrieval:
        answer = generate_without_retrieval(query)
        return {"answer": answer, "mode": "no_retrieval", "attempts": 1}

    # 2. Retrieve and filter for relevance
    chunks = retrieve(query, collection_name, k=5)
    relevant_chunks = [c for c in chunks if grade_relevance(query, c["text"])]
    print(f"Relevant chunks: {len(relevant_chunks)}/{len(chunks)}")

    if not relevant_chunks:
        answer = generate_without_retrieval(query)
        return {"answer": answer, "mode": "retrieval_failed", "attempts": 1}

    # 3. Generate and critique
    for attempt in range(1, max_retries + 2):
        answer = generate_with_retrieval(query, relevant_chunks)
        context = " ".join(c["text"] for c in relevant_chunks)
        critique = critique_answer(query, answer, context)

        print(f"Attempt {attempt}: supported={critique['supported']}, useful={critique['useful']}")

        if critique["supported"] and critique["useful"]:
            return {"answer": answer, "mode": "retrieval_grounded", "attempts": attempt}

        if attempt <= max_retries:
            print(f"Retrying — issue: {critique.get('issue', 'unknown')}")

    return {"answer": answer, "mode": "best_effort", "attempts": max_retries + 1}`;function f(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Self-RAG"}),e.jsx("p",{children:"Standard and corrective RAG always retrieve documents for every query, even when retrieval is unnecessary or harmful. Self-RAG introduces a more sophisticated approach: the model itself decides whether to retrieve, evaluates the retrieved documents, and critiques its own generated response — all through special tokens inserted into the generation process. The result is a system that retrieves adaptively rather than unconditionally."}),e.jsx(r,{term:"Self-RAG",definition:"Self-RAG is a framework where a language model is fine-tuned to generate special reflection tokens alongside its output. These tokens encode four decisions: whether to retrieve (Retrieve), whether each retrieved document is relevant (ISREL), whether the generated response is supported by the retrieved documents (ISSUP), and whether the response is useful to the user (ISUSE). The model uses these tokens to control its own retrieval and to select the best among multiple generated responses."}),e.jsx("h2",{children:"The Four Reflection Tokens"}),e.jsx("h3",{children:"Retrieve — Should I retrieve?"}),e.jsx("p",{children:"Not every query benefits from retrieval. Simple arithmetic, common knowledge, and conversational responses do not need documents. The Retrieve token lets the model decide per-query whether to invoke the retrieval system. This reduces unnecessary retrieval overhead and avoids cases where retrieved documents confuse rather than help."}),e.jsx("h3",{children:"ISREL — Is this document relevant?"}),e.jsx("p",{children:"For each retrieved document, the model generates an ISREL token assessing whether the document contains information relevant to the query. Irrelevant documents are filtered out before generation, preventing noise from entering the context."}),e.jsx("h3",{children:"ISSUP — Is my answer supported?"}),e.jsx("p",{children:"After generating a response segment grounded in a specific document, the model generates an ISSUP token assessing whether the generated text is actually supported by that document. This is a self-consistency check against hallucination — the model flags when it has generated content beyond what the document supports."}),e.jsx("h3",{children:"ISUSE — Is this response useful?"}),e.jsx("p",{children:"The model also generates an ISUSE token rating the overall usefulness of its response. When multiple response candidates are generated (one per relevant document), ISUSE scores are used to select the best one."}),e.jsx("h2",{children:"Fine-Tuning vs. Prompted Self-RAG"}),e.jsx("p",{children:"The original Self-RAG paper trains a custom model to generate reflection tokens as part of its output — the tokens are built into the model's vocabulary via supervised fine-tuning on data where a teacher model annotated when retrieval was needed and which responses were grounded. This requires a fine-tuning dataset and training run."}),e.jsx("p",{children:"For teams without fine-tuning resources, the reflection token decisions can be approximated with prompted LLM calls — separate API calls that ask the model to make each decision. This is slower and more expensive than fine-tuned token generation but requires no training and works with any capable base model. The code example below implements this prompted approach."}),e.jsx("h2",{children:"Self-RAG vs. CRAG"}),e.jsx("p",{children:"Both Self-RAG and CRAG add document quality evaluation and retrieval fallbacks. The key differences are:"}),e.jsxs("ul",{children:[e.jsx("li",{children:"Self-RAG operates at the token/segment level — the model generates a piece of its response, then reflects on whether it needs retrieval for the next piece. CRAG operates at the query level — retrieve, then grade all documents at once."}),e.jsx("li",{children:"Self-RAG can interleave retrieval with generation, retrieving different documents for different parts of a long response. CRAG performs a single retrieval at the start."}),e.jsx("li",{children:"Self-RAG's full power requires a fine-tuned model. CRAG works well with prompted approaches using standard models."})]}),e.jsx(s,{type:"historical",title:"Self-RAG paper",children:'Self-RAG was introduced in "Self-RAG: Learning to Retrieve, Generate, and Critique through Self-Reflection" (Asai et al., 2023). The fine-tuned Llama 2 variant achieved strong performance on knowledge-intensive tasks while generating fewer hallucinated facts than standard RAG. The paper is notable for demonstrating that a model can be taught meta-cognitive skills (knowing when it knows, knowing when it needs help) through careful fine-tuning.'}),e.jsx(n,{title:"Start with prompted Self-RAG before committing to fine-tuning",children:"Implementing prompted Self-RAG (using LLM calls for each reflection decision) lets you validate the architecture's benefit on your use case before investing in fine- tuning. If the prompted version improves faithfulness and reduces unnecessary retrievals by more than 20%, the investment in fine-tuning a custom model is likely justified. If the gains are marginal, CRAG or standard RAG with a good threshold may be sufficient."}),e.jsx("h2",{children:"Simulated Self-RAG with Prompting"}),e.jsx(t,{title:"Self-RAG Pipeline via Prompted Reflection",tabs:{python:g}}),e.jsx("p",{children:"The implementation above approximates Self-RAG's four reflection token decisions with separate prompted LLM calls. The pipeline decides whether retrieval is needed, grades document relevance, generates the answer, and critiques it for support and usefulness — retrying if the critique fails. While not as efficient as a fine-tuned model generating tokens inline, this approach captures the core Self-RAG logic and is a practical starting point for teams using frontier models via API."})]})}const N=Object.freeze(Object.defineProperty({__proto__:null,default:f},Symbol.toStringTag,{value:"Module"})),y=`import json
from anthropic import Anthropic
import voyageai
import chromadb

client = Anthropic()
voyage = voyageai.Client()
chroma = chromadb.PersistentClient(path="./vector_store")

def classify_query_complexity(query: str) -> dict:
    """
    Classify the query into one of three complexity levels:
    - simple: factual, single-hop, answerable from parametric memory
    - moderate: requires one retrieval step
    - complex: requires multi-step or iterative retrieval
    """
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=128,
        system='Classify query complexity. Return JSON: {"complexity": "simple"|"moderate"|"complex", "reasoning": "..."}',
        messages=[{
            "role": "user",
            "content": (
                f"Query: {query}\\n\\n"
                "Classify this query:\\n"
                "- simple: common knowledge, no retrieval needed\\n"
                "- moderate: needs one retrieval step from a knowledge base\\n"
                "- complex: needs multiple retrieval steps or cross-document synthesis"
            )
        }],
    )
    raw = response.content[0].text.strip().strip("json").strip("")
    return json.loads(raw)

def retrieve(query: str, collection_name: str, k: int = 5) -> list[dict]:
    result = voyage.embed([query], model="voyage-3")
    collection = chroma.get_collection(collection_name)
    res = collection.query(query_embeddings=[result.embeddings[0]], n_results=k)
    return [{"text": doc, "source": meta.get("source", "")}
            for doc, meta in zip(res["documents"][0], res["metadatas"][0])]

def single_step_rag(query: str, collection_name: str) -> str:
    """Standard single-step retrieval + generation."""
    chunks = retrieve(query, collection_name, k=5)
    context = "\\n\\n".join(f"[{i+1}] {c['text']}" for i, c in enumerate(chunks))
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=512,
        system="Answer using ONLY the provided context. Cite [1], [2], etc.",
        messages=[{"role": "user", "content": f"Context:\\n{context}\\n\\nQ: {query}"}],
    )
    return response.content[0].text

def iterative_rag(
    query: str,
    collection_name: str,
    max_iterations: int = 3,
) -> str:
    """
    Iterative retrieval: generate intermediate answer, identify knowledge gaps,
    retrieve for gaps, repeat until answer is complete.
    """
    all_chunks = []
    current_query = query

    for iteration in range(1, max_iterations + 1):
        new_chunks = retrieve(current_query, collection_name, k=3)
        all_chunks.extend(new_chunks)

        context = "\\n\\n".join(
            f"[{i+1}] {c['text']}" for i, c in enumerate(all_chunks[:8])
        )

        # Ask model if it has enough information or needs more
        check_response = client.messages.create(
            model="claude-opus-4-5",
            max_tokens=256,
            system='Return JSON: {"complete": true|false, "follow_up_query": "..." or null, "partial_answer": "..."}',
            messages=[{
                "role": "user",
                "content": (
                    f"Context so far:\\n{context}\\n\\n"
                    f"Original question: {query}\\n\\n"
                    "Do you have enough information to fully answer this question? "
                    "If not, what follow-up query would get the missing information?"
                )
            }],
        )
        raw = check_response.content[0].text.strip().strip("json").strip("")
        status = json.loads(raw)
        print(f"Iteration {iteration}: complete={status['complete']}")

        if status["complete"] or not status.get("follow_up_query"):
            break
        current_query = status["follow_up_query"]

    # Final generation
    final_context = "\\n\\n".join(
        f"[{i+1}] {c['text']}" for i, c in enumerate(all_chunks[:8])
    )
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=768,
        system="Synthesise a complete answer using all provided context. Cite [1], [2], etc.",
        messages=[{"role": "user", "content": f"Context:\\n{final_context}\\n\\nQuestion: {query}"}],
    )
    return response.content[0].text

def adaptive_rag(query: str, collection_name: str) -> dict:
    """
    Adaptive RAG: route to the appropriate retrieval strategy based on query complexity.
    """
    classification = classify_query_complexity(query)
    complexity = classification["complexity"]
    print(f"Query complexity: {complexity} — {classification['reasoning']}")

    if complexity == "simple":
        # Direct generation, no retrieval
        response = client.messages.create(
            model="claude-opus-4-5",
            max_tokens=256,
            messages=[{"role": "user", "content": query}],
        )
        answer = response.content[0].text
        strategy = "no_retrieval"

    elif complexity == "moderate":
        answer = single_step_rag(query, collection_name)
        strategy = "single_step"

    else:  # complex
        answer = iterative_rag(query, collection_name, max_iterations=3)
        strategy = "iterative"

    return {"answer": answer, "strategy": strategy, "complexity": complexity}

# Example
# result = adaptive_rag("What is 2+2?", "docs")           # → no_retrieval
# result = adaptive_rag("What is RAG?", "docs")            # → single_step
# result = adaptive_rag("Compare all RAG variants", "docs") # → iterative`;function v(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Adaptive RAG"}),e.jsx("p",{children:'A one-size-fits-all retrieval strategy is inefficient. Simple factual queries ("What does API stand for?") do not need retrieval at all. Moderate queries need one retrieval step. Complex multi-part questions need iterative retrieval across multiple documents. Adaptive RAG routes each query to the retrieval strategy appropriate for its complexity, optimising for both quality and cost.'}),e.jsx(r,{term:"Adaptive RAG",definition:"Adaptive RAG is a meta-framework that dynamically selects the retrieval strategy for each incoming query based on a query complexity classification. Simple queries skip retrieval and go directly to generation from parametric knowledge. Moderate queries use single-step standard RAG. Complex queries invoke iterative or multi-hop retrieval. The classifier routes queries to the right strategy, reducing unnecessary retrieval for simple queries and enabling thorough retrieval for complex ones."}),e.jsx("h2",{children:"The Three Retrieval Strategies"}),e.jsx("h3",{children:"Strategy 1 — No Retrieval (Parametric Generation)"}),e.jsx("p",{children:"Many queries are answerable from the model's training knowledge without any retrieval: common definitions, well-known facts, simple conversational responses, arithmetic. For these, retrieval adds latency and cost without improving quality. Adaptive RAG skips retrieval entirely for this class of queries."}),e.jsx("h3",{children:"Strategy 2 — Single-Step RAG"}),e.jsx("p",{children:"Most domain-specific queries about the knowledge base need exactly one retrieval step: embed the query, retrieve top-k chunks, generate an answer. This is the standard RAG pipeline. It handles the majority of production queries efficiently."}),e.jsx("h3",{children:"Strategy 3 — Iterative RAG"}),e.jsx("p",{children:"Complex questions — those requiring synthesis across multiple documents, multi-hop reasoning, or answers that depend on first answering sub-questions — need iterative retrieval. The model retrieves, generates a partial answer, identifies what is still missing, formulates a follow-up query, retrieves again, and repeats until it has enough information. This is slower and more expensive but produces significantly better answers for genuinely complex queries."}),e.jsx("h2",{children:"Query Complexity Classification"}),e.jsx("p",{children:"The adaptive routing decision is made by a query classifier that runs before retrieval. This classifier can be implemented as:"}),e.jsxs("ul",{children:[e.jsxs("li",{children:[e.jsx("strong",{children:"Prompted LLM"}),": Ask a fast model (Claude Haiku) to classify the query into simple/moderate/complex. Accurate but adds ~100ms latency."]}),e.jsxs("li",{children:[e.jsx("strong",{children:"Fine-tuned classifier"}),": Train a small text classifier on labelled query examples. Near-zero latency and lower cost per query. Requires a labelled dataset."]}),e.jsxs("li",{children:[e.jsx("strong",{children:"Rule-based heuristics"}),': Word count (short queries → no retrieval), presence of comparison words ("compare", "contrast", "versus" → iterative), question words ("what is" → single-step). Fast but brittle.']})]}),e.jsx("h2",{children:"Benefits and Trade-offs"}),e.jsx("p",{children:"The primary benefit of Adaptive RAG is cost efficiency. A production system might find that 30% of queries are simple (skip retrieval), 60% are moderate (single-step), and only 10% are complex (iterative). Adaptive routing reduces embedding and vector search costs by 30% and eliminates 90% of expensive iterative retrieval overhead compared to always using iterative RAG."}),e.jsx("p",{children:"The trade-off is the classification step itself. Misclassifying a moderate query as simple produces a lower-quality answer. Misclassifying a simple query as complex wastes resources. Tune the classifier conservatively: when in doubt, classify upward (simple → moderate, moderate → complex) to preserve quality at the cost of slightly higher resource usage."}),e.jsx(s,{type:"tip",title:"Use query logs to calibrate the classifier",children:"After deploying adaptive RAG, log the complexity classification alongside user satisfaction signals (thumbs up/down, session continuation). Queries classified as 'simple' that received negative feedback are likely misclassified moderates — add them as training examples for the classifier. Over time, your classifier improves specifically for your query distribution."}),e.jsx(n,{title:"Instrument each strategy separately in your metrics",children:"Track quality metrics (faithfulness, answer relevancy) and performance metrics (latency, cost) separately for each routing path. If the no-retrieval path has low faithfulness, your classifier is routing too many queries there. If iterative is rarely used, consider whether complex queries are being misclassified as moderate. Separate instrumentation makes routing quality visible and improvable."}),e.jsx("h2",{children:"Adaptive RAG Implementation"}),e.jsx(t,{title:"Adaptive RAG with Query Complexity Routing",tabs:{python:y}}),e.jsx("p",{children:"The implementation routes queries through three strategies based on a prompted complexity classifier. The iterative strategy uses a follow-up query loop, asking the model whether its accumulated context is sufficient and generating a refined query for missing information. The examples in the comments illustrate how the three routing paths handle qualitatively different queries — from trivial arithmetic to complex comparative analysis."})]})}const H=Object.freeze(Object.defineProperty({__proto__:null,default:v},Symbol.toStringTag,{value:"Module"})),_=`import json
from anthropic import Anthropic
import voyageai
import chromadb

client = Anthropic()
voyage = voyageai.Client()
chroma = chromadb.PersistentClient(path="./vector_store")

def retrieve(query: str, collection_name: str, k: int = 3) -> list[dict]:
    result = voyage.embed([query], model="voyage-3")
    collection = chroma.get_collection(collection_name)
    res = collection.query(query_embeddings=[result.embeddings[0]], n_results=k)
    return [{"text": doc, "source": meta.get("source", "")}
            for doc, meta in zip(res["documents"][0], res["metadatas"][0])]

def decompose_question(question: str) -> list[str]:
    """Decompose a multi-hop question into ordered sub-questions."""
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=256,
        system='Decompose questions. Return JSON: {"subquestions": ["q1", "q2", ...]}',
        messages=[{
            "role": "user",
            "content": (
                f"Decompose this multi-hop question into 2-4 simpler sub-questions "
                f"that must be answered in order.\\n\\nQuestion: {question}"
            )
        }],
    )
    raw = response.content[0].text.strip().strip("json").strip("")
    return json.loads(raw).get("subquestions", [question])

def answer_subquestion(
    subquestion: str,
    context_so_far: str,
    chunks: list[dict],
) -> str:
    """Answer a sub-question given accumulated context."""
    chunk_context = "\\n\\n".join(f"[{i+1}] {c['text']}" for i, c in enumerate(chunks))
    full_context = f"{context_so_far}\\n\\n---Retrieved---\\n\\n{chunk_context}" if context_so_far else chunk_context

    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=256,
        system="Answer the sub-question using the provided context. Be concise.",
        messages=[{
            "role": "user",
            "content": f"Context:\\n{full_context}\\n\\nSub-question: {subquestion}"
        }],
    )
    return response.content[0].text.strip()

def multi_hop_rag(question: str, collection_name: str) -> dict:
    """
    Multi-hop RAG: decompose question, retrieve and answer each hop in sequence,
    then synthesise the final answer.
    """
    # 1. Decompose
    subquestions = decompose_question(question)
    print(f"Multi-hop chain ({len(subquestions)} hops):")
    for i, sq in enumerate(subquestions, 1):
        print(f"  Hop {i}: {sq}")

    # 2. Answer each hop sequentially, passing intermediate answers forward
    accumulated_context = ""
    hop_answers = []

    for i, subq in enumerate(subquestions, 1):
        # Use intermediate answers to refine the retrieval query for the next hop
        retrieval_query = subq
        if hop_answers:
            retrieval_query = f"{subq} (context: {' '.join(hop_answers[-2:])})"

        chunks = retrieve(retrieval_query, collection_name, k=3)
        hop_answer = answer_subquestion(subq, accumulated_context, chunks)
        hop_answers.append(hop_answer)
        accumulated_context += f"\\nHop {i} ({subq}): {hop_answer}"
        print(f"  Hop {i} answer: {hop_answer[:80]}...")

    # 3. Final synthesis
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=512,
        system="Synthesise a final answer from the step-by-step reasoning chain.",
        messages=[{
            "role": "user",
            "content": (
                f"Original question: {question}\\n\\n"
                f"Reasoning chain:\\n{accumulated_context}\\n\\n"
                "Provide a complete, synthesised final answer."
            )
        }],
    )

    return {
        "question": question,
        "subquestions": subquestions,
        "hop_answers": hop_answers,
        "final_answer": response.content[0].text,
    }

# Example
# result = multi_hop_rag(
#     "Who founded the company that makes the model used in the most popular RAG framework?",
#     "tech_docs",
# )`;function w(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Multi-Hop Reasoning"}),e.jsx("p",{children:`Many real-world questions cannot be answered from a single document. "What is the capital of the country that invented the printing press?" requires first knowing which country invented the printing press (Germany), then knowing Germany's capital (Berlin). Each "hop" retrieves information that enables the next retrieval step. Multi-hop RAG chains retrieval operations to answer questions that require connecting facts across multiple documents.`}),e.jsx(r,{term:"Multi-Hop Retrieval",definition:"Multi-hop retrieval is a RAG strategy where answering a question requires multiple sequential retrieval steps, each building on the results of the previous one. The answer to an intermediate sub-question becomes context for the next retrieval query, forming a chain of reasoning steps that eventually assembles enough information to answer the original question."}),e.jsx("h2",{children:"When Multi-Hop Retrieval Is Needed"}),e.jsx("p",{children:"A query requires multi-hop reasoning when:"}),e.jsxs("ul",{children:[e.jsx("li",{children:'The question contains bridging entities — "the company that makes X" requires first finding what company makes X, then retrieving information about that company.'}),e.jsx("li",{children:`Comparison questions — "Which is faster: A or B?" requires separate retrievals for A's performance and B's performance.`}),e.jsx("li",{children:'Temporal reasoning — "What happened after event X?" requires first retrieving when event X occurred, then retrieving events in that time period.'}),e.jsx("li",{children:'Hierarchical questions — "What does the parent company of X do?" requires identifying the parent company, then retrieving information about it.'})]}),e.jsx("h2",{children:"The Multi-Hop Chain"}),e.jsx("p",{children:"A multi-hop pipeline follows this pattern:"}),e.jsxs("ol",{children:[e.jsxs("li",{children:[e.jsx("strong",{children:"Decompose"}),": Break the original question into an ordered sequence of sub-questions. Each sub-question should be answerable independently, and its answer should enable the next sub-question."]}),e.jsxs("li",{children:[e.jsx("strong",{children:"Retrieve for Hop 1"}),": Use the first sub-question as the retrieval query. Get the top-k relevant chunks."]}),e.jsxs("li",{children:[e.jsx("strong",{children:"Answer Hop 1"}),': Generate an intermediate answer for the first sub-question. This answer becomes a "bridge entity" — a piece of information needed for the next hop.']}),e.jsxs("li",{children:[e.jsx("strong",{children:"Refine query for Hop 2"}),': Incorporate the Hop 1 answer into the Hop 2 retrieval query. "What is the revenue of [company identified in Hop 1]?"']}),e.jsxs("li",{children:[e.jsx("strong",{children:"Repeat"})," until all hops are complete, then synthesise a final answer from all accumulated intermediate answers."]})]}),e.jsx("h2",{children:"Challenges in Multi-Hop RAG"}),e.jsx("h3",{children:"Error Propagation"}),e.jsx("p",{children:"A wrong intermediate answer at Hop 1 propagates incorrectness into Hop 2 and beyond. The final answer can only be as good as the weakest hop. Grading intermediate answers for confidence and triggering fallbacks when confidence is low helps mitigate this."}),e.jsx("h3",{children:"Chain Length"}),e.jsx("p",{children:"Each additional hop adds latency (a retrieval + generation cycle) and cost. Most real-world multi-hop questions require 2–4 hops. Beyond 4 hops, error propagation and latency make the approach impractical — consider graph-based retrieval (GraphRAG) for deeper chains."}),e.jsx("h3",{children:"Sub-question Decomposition Quality"}),e.jsx("p",{children:"The decomposition step is the most brittle. If the sub-questions do not correctly capture the reasoning chain, subsequent hops retrieve irrelevant information. Use few-shot examples in the decomposition prompt to show the LLM how to decompose questions in your domain."}),e.jsx(s,{type:"tip",title:"Log the full reasoning chain for debugging",children:"Multi-hop pipelines are hard to debug because errors can occur at any hop. Log the sub-questions, retrieved chunks per hop, and intermediate answers for every request. When the final answer is wrong, trace back through the log to find which hop failed — whether it was a bad decomposition, a failed retrieval, or a wrong intermediate answer. This tracing capability is essential for improving multi-hop pipeline quality."}),e.jsx(n,{title:"Validate bridge entities before the next hop",children:`After answering a hop, check whether the intermediate answer is specific enough to use as a retrieval query for the next hop. If Hop 1 answers "I don't know" or produces a vague response, the next hop's retrieval will fail. Add an intermediate validation step: if the hop answer is insufficiently specific (e.g., fails to name an entity), trigger a fallback (broader retrieval or abstention) rather than proceeding with a bad bridge entity.`}),e.jsx("h2",{children:"Multi-Hop RAG in Practice"}),e.jsx(t,{title:"Multi-Hop RAG Pipeline",tabs:{python:_}}),e.jsx("p",{children:"The implementation decomposes the question into sub-questions, answers each hop sequentially using the accumulated context from previous hops, and synthesises a final answer from the full reasoning chain. The key detail is that each hop's retrieval query is enriched with context from previous hop answers — this is what enables the chain to navigate across documents rather than retrieving the same documents repeatedly."})]})}const W=Object.freeze(Object.defineProperty({__proto__:null,default:w},Symbol.toStringTag,{value:"Module"})),x=`import json
from anthropic import Anthropic
import networkx as nx

client = Anthropic()

# ---- Build a Knowledge Graph from Documents ----

def extract_entities_and_relations(text: str) -> dict:
    """Extract (entity, relation, entity) triples from text using Claude."""
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=512,
        system='Extract knowledge graph triples. Return JSON: {"triples": [{"subject": "...", "relation": "...", "object": "..."}]}',
        messages=[{
            "role": "user",
            "content": (
                f"Extract all named entities and their relationships from this text "
                f"as (subject, relation, object) triples.\\n\\nText: {text}"
            )
        }],
    )
    raw = response.content[0].text.strip().strip("json").strip("")
    return json.loads(raw)

def build_knowledge_graph(documents: list[dict]) -> nx.DiGraph:
    """
    Build a directed knowledge graph from a collection of documents
    by extracting entity-relation-entity triples.
    """
    G = nx.DiGraph()

    for doc in documents:
        extracted = extract_entities_and_relations(doc["text"])
        for triple in extracted.get("triples", []):
            subject = triple["subject"].lower().strip()
            relation = triple["relation"].lower().strip()
            obj = triple["object"].lower().strip()

            # Add nodes with document source as metadata
            G.add_node(subject, sources=[doc["source"]])
            if obj in G:
                G.nodes[obj].setdefault("sources", []).append(doc["source"])
            else:
                G.add_node(obj, sources=[doc["source"]])

            # Add directed edge with relation label
            G.add_edge(subject, obj, relation=relation, source=doc["source"])

    return G

# ---- Graph-Enhanced Retrieval ----

def graph_retrieve(
    query: str,
    G: nx.DiGraph,
    seed_entities: list[str],
    max_hops: int = 2,
    max_nodes: int = 20,
) -> list[dict]:
    """
    Retrieve context by traversing the knowledge graph from seed entities.
    Returns a list of (subject, relation, object) triples as context.
    """
    visited = set()
    context_triples = []
    frontier = set(e.lower() for e in seed_entities if e.lower() in G)

    for hop in range(max_hops):
        next_frontier = set()
        for node in frontier:
            if node in visited or len(context_triples) >= max_nodes:
                break
            visited.add(node)

            # Outgoing edges
            for neighbor in G.successors(node):
                edge_data = G[node][neighbor]
                context_triples.append({
                    "subject": node,
                    "relation": edge_data.get("relation", "related_to"),
                    "object": neighbor,
                    "source": edge_data.get("source", ""),
                })
                next_frontier.add(neighbor)

            # Incoming edges (reverse traversal)
            for predecessor in G.predecessors(node):
                edge_data = G[predecessor][node]
                context_triples.append({
                    "subject": predecessor,
                    "relation": edge_data.get("relation", "related_to"),
                    "object": node,
                    "source": edge_data.get("source", ""),
                })
                next_frontier.add(predecessor)

        frontier = next_frontier - visited

    return context_triples

def extract_seed_entities(query: str) -> list[str]:
    """Extract named entities from the query to use as graph traversal seeds."""
    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=128,
        system='Extract entities. Return JSON: {"entities": ["entity1", "entity2"]}',
        messages=[{"role": "user", "content": f"Extract all named entities from: {query}"}],
    )
    raw = response.content[0].text.strip().strip("json").strip("")
    return json.loads(raw).get("entities", [])

def graph_rag(query: str, G: nx.DiGraph) -> str:
    """Full GraphRAG pipeline: entity extraction → graph traversal → generation."""
    seeds = extract_seed_entities(query)
    print(f"Seed entities: {seeds}")

    triples = graph_retrieve(query, G, seeds, max_hops=2)
    print(f"Retrieved {len(triples)} graph triples")

    if not triples:
        return "Could not find relevant information in the knowledge graph."

    # Format triples as context
    context = "\\n".join(
        f"- {t['subject']} --[{t['relation']}]--> {t['object']}"
        for t in triples[:20]
    )

    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=512,
        system="Answer using the provided knowledge graph triples as context.",
        messages=[{"role": "user", "content": f"Knowledge Graph:\\n{context}\\n\\nQuestion: {query}"}],
    )
    return response.content[0].text

# Example
docs = [
    {"text": "Anthropic was founded by Dario Amodei and Daniela Amodei. Anthropic created Claude.", "source": "wiki"},
    {"text": "Claude is a large language model trained with Constitutional AI. Claude is used in RAG systems.", "source": "anthropic_docs"},
    {"text": "Constitutional AI was developed at Anthropic to make models safer and more helpful.", "source": "paper"},
]
# G = build_knowledge_graph(docs)
# answer = graph_rag("Who created Claude and what method was used to train it?", G)`;function b(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"GraphRAG"}),e.jsx("p",{children:'Standard vector retrieval treats documents as independent bags of text and returns the most similar chunks. It cannot traverse relationships between entities across documents. If you need to answer "What are all the products made by the company that acquired CompanyX?" you need to follow a chain of relationships: CompanyX → acquired_by → AcquirerY → makes → [products]. Knowledge graphs make these relational paths explicit and traversable.'}),e.jsx(r,{term:"GraphRAG",definition:"GraphRAG is a retrieval approach that combines knowledge graph construction with vector search. Documents are processed to extract entities and their relationships, forming a structured knowledge graph. At query time, entities mentioned in the query serve as starting nodes for graph traversal, retrieving both the directly relevant facts and their connected context through multi-hop graph walks. This enables relational and associative retrieval that flat vector search cannot achieve."}),e.jsx("h2",{children:"Knowledge Graph Construction"}),e.jsx("p",{children:"Building a knowledge graph from unstructured documents requires entity and relation extraction — also called information extraction or triple extraction. Each sentence in a document is parsed to produce (Subject, Relation, Object) triples:"}),e.jsxs("ul",{children:[e.jsx("li",{children:'"Anthropic was founded by Dario Amodei" → (Anthropic, founded_by, Dario Amodei)'}),e.jsx("li",{children:'"Claude uses Constitutional AI" → (Claude, uses, Constitutional AI)'}),e.jsx("li",{children:'"Constitutional AI was developed at Anthropic" → (Constitutional AI, developed_at, Anthropic)'})]}),e.jsx("p",{children:"These triples form the edges of a directed graph. LLMs are highly capable at triple extraction — a simple prompt asking for (subject, relation, object) tuples produces usable output for most document types."}),e.jsx("h2",{children:"Graph Traversal for Retrieval"}),e.jsx("p",{children:"At query time, named entities in the query are identified and used as starting nodes (seeds) for graph traversal. A breadth-first walk from each seed node collects neighbouring nodes and their edge labels within k hops. The collected triples are formatted as context for the LLM generation step."}),e.jsx("p",{children:'Graph traversal naturally surfaces multi-hop relational context. Starting from "Anthropic" and traversing 2 hops retrieves both "Anthropic → founded_by → Dario Amodei" and "Anthropic → created → Claude → uses → Constitutional AI" — connecting entities across documents in a way that vector search cannot.'}),e.jsx("h2",{children:"Microsoft GraphRAG"}),e.jsx("p",{children:"Microsoft's GraphRAG paper and open-source implementation extends the basic approach with community detection and hierarchical summarisation. After building the entity graph, it applies graph community detection (Leiden algorithm) to identify clusters of closely related entities, then generates natural language summaries for each community. At query time, the system can retrieve at multiple granularities: specific entities, community summaries, or global corpus summaries. This makes it particularly powerful for large corpora where understanding themes and structure is as important as finding specific facts."}),e.jsx("h2",{children:"When to Use GraphRAG"}),e.jsx("p",{children:"GraphRAG adds significant complexity compared to standard vector RAG:"}),e.jsxs("ul",{children:[e.jsx("li",{children:"Graph construction requires an LLM call per document chunk (expensive at scale)."}),e.jsx("li",{children:"The graph must be rebuilt or updated when documents change."}),e.jsx("li",{children:"Graph traversal logic is more complex than a vector similarity query."})]}),e.jsx("p",{children:"Use GraphRAG when your queries are inherently relational — they traverse entity relationships rather than seeking textually similar passages. Typical use cases: knowledge bases about organisations and their relationships, scientific literature where paper-cites-paper graphs matter, product catalogs with hierarchical category and relationship structures."}),e.jsx(s,{type:"tip",title:"Combine vector and graph retrieval",children:"GraphRAG and vector RAG are complementary. Vector retrieval finds relevant text passages; graph traversal finds relevant entity relationships. A hybrid approach uses vector search to find the seed entities (the most relevant chunks) and then expands the context with graph traversal from those entities. This is often more effective than either approach alone."}),e.jsx(n,{title:"Store source provenance on every graph edge",children:"Every edge in your knowledge graph should record which document it came from. This enables two things: (1) you can return source citations for graph-retrieved facts, just as you do for vector-retrieved chunks; and (2) you can selectively update or remove edges when a source document changes, without rebuilding the entire graph. Source provenance is essential for both trust and maintainability."}),e.jsx("h2",{children:"GraphRAG with NetworkX"}),e.jsx(t,{title:"Knowledge Graph Construction and GraphRAG",tabs:{python:x}}),e.jsx("p",{children:"The implementation builds a knowledge graph using Claude to extract entity-relation triples from documents, stores the graph with NetworkX, and implements graph traversal from query-extracted seed entities. For production scale, replace NetworkX with a dedicated graph database (Neo4j, Amazon Neptune, or Weaviate's Knowledge Graph module) that can handle millions of nodes and edges with efficient traversal."})]})}const U=Object.freeze(Object.defineProperty({__proto__:null,default:b},Symbol.toStringTag,{value:"Module"})),q=`import json
from anthropic import Anthropic
import voyageai
import chromadb

client = Anthropic()
voyage = voyageai.Client()
chroma = chromadb.PersistentClient(path="./vector_store")

def retrieve(query: str, collection_name: str, k: int = 3) -> list[dict]:
    result = voyage.embed([query], model="voyage-3")
    collection = chroma.get_collection(collection_name)
    res = collection.query(query_embeddings=[result.embeddings[0]], n_results=k)
    return [{"text": doc} for doc in res["documents"][0]]

def ircot(
    question: str,
    collection_name: str,
    max_steps: int = 4,
) -> dict:
    """
    IRCoT: Interleaved Retrieval with Chain-of-Thought.

    The model generates reasoning steps one at a time. After each step,
    if it identifies a knowledge gap, it retrieves relevant documents
    and continues reasoning with the enriched context.
    """
    reasoning_chain = []
    all_retrieved_docs = []

    # Initial retrieval
    initial_chunks = retrieve(question, collection_name, k=3)
    all_retrieved_docs.extend(initial_chunks)

    for step in range(1, max_steps + 1):
        # Build context from accumulated documents and reasoning
        doc_context = "\\n\\n".join(f"[{i+1}] {c['text']}" for i, c in enumerate(all_retrieved_docs[:6]))
        reasoning_so_far = "\\n".join(f"Step {i+1}: {r['thought']}" for i, r in enumerate(reasoning_chain))

        prompt = f"""Question: {question}

Retrieved documents:
{doc_context}

{"Previous reasoning:" + chr(10) + reasoning_so_far if reasoning_so_far else ""}

Generate the next reasoning step. If you need more information, identify what to search for.
Return JSON: {{
  "thought": "reasoning step text",
  "needs_retrieval": true|false,
  "search_query": "query string if needs_retrieval is true" or null,
  "final_answer": "answer string if this is the last step" or null
}}"""

        response = client.messages.create(
            model="claude-opus-4-5",
            max_tokens=256,
            system="You reason step-by-step over retrieved documents. Return JSON.",
            messages=[{"role": "user", "content": prompt}],
        )
        raw = response.content[0].text.strip().strip("json").strip("")
        step_result = json.loads(raw)

        thought = step_result.get("thought", "")
        reasoning_chain.append({"step": step, "thought": thought})
        print(f"Step {step}: {thought[:80]}...")

        # If model has a final answer, stop
        if step_result.get("final_answer"):
            return {
                "question": question,
                "reasoning_chain": reasoning_chain,
                "answer": step_result["final_answer"],
                "steps": step,
            }

        # If model needs retrieval, retrieve and continue
        if step_result.get("needs_retrieval") and step_result.get("search_query"):
            new_chunks = retrieve(step_result["search_query"], collection_name, k=3)
            all_retrieved_docs.extend(new_chunks)
            print(f"  → Retrieved {len(new_chunks)} new docs for: {step_result['search_query']}")

    # Fallback: generate final answer with all accumulated context
    doc_context = "\\n\\n".join(f"[{i+1}] {c['text']}" for i, c in enumerate(all_retrieved_docs[:8]))
    reasoning_so_far = "\\n".join(f"Step {i+1}: {r['thought']}" for i, r in enumerate(reasoning_chain))

    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=512,
        system="Synthesise a final answer from the reasoning chain and documents.",
        messages=[{"role": "user", "content": f"Question: {question}\\n\\nReasoning:\\n{reasoning_so_far}\\n\\nDocuments:\\n{doc_context}\\n\\nFinal answer:"}],
    )
    return {
        "question": question,
        "reasoning_chain": reasoning_chain,
        "answer": response.content[0].text,
        "steps": max_steps,
    }`;function k(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Multi-Hop Architectures"}),e.jsx("p",{children:"The previous sections covered the concepts of multi-hop reasoning and GraphRAG. This section surveys specific architectural patterns — IRCoT, FLARE, and ReAct-RAG — that provide principled frameworks for implementing multi-step retrieval and reasoning. Each pattern makes different trade-offs between complexity, latency, and reasoning depth."}),e.jsx(r,{term:"IRCoT (Interleaved Retrieval with Chain-of-Thought)",definition:"IRCoT interleaves chain-of-thought reasoning steps with retrieval operations. The model generates reasoning steps one at a time, and after each step, the system checks whether the model identified a knowledge gap. If so, a targeted retrieval is performed, and the model continues reasoning with the new documents in context. This interleaving allows retrieval to be guided by the model's own emerging understanding of what it needs."}),e.jsx("h2",{children:"IRCoT — Interleaved Retrieval with Chain-of-Thought"}),e.jsx("p",{children:'In standard RAG, retrieval happens once at the start. In IRCoT, retrieval and reasoning alternate: reason → identify gap → retrieve → reason again. The key insight is that chain-of-thought reasoning naturally surfaces what information is missing — "I know X, but I need to know Y to answer the question" — which makes an excellent retrieval query.'}),e.jsx("p",{children:"IRCoT is particularly effective for questions that require connecting facts across multiple documents, because the model's intermediate reasoning provides precise, contextualised retrieval queries rather than using the original question repeatedly. Each retrieval query is conditioned on what the model has already reasoned about."}),e.jsx("h2",{children:"FLARE — Forward-Looking Active Retrieval"}),e.jsx("p",{children:"FLARE (Forward-Looking Active REtrieval augmented generation) takes a different approach: the model generates its response token by token and triggers retrieval when it is about to generate low-confidence tokens. When the model would generate a token that might be uncertain (measured by low probability mass on the top tokens), it pauses, formulates a retrieval query based on the upcoming content it was about to generate, retrieves relevant documents, and then regenerates that portion of its response with the retrieved context."}),e.jsx("p",{children:"FLARE requires access to the model's token probabilities (log-probs), which is available from many model APIs. It is best suited for long-form generation tasks where different portions of the response require different factual knowledge."}),e.jsx("h2",{children:"ReAct-RAG (Reasoning + Acting)"}),e.jsx("p",{children:"ReAct combines the ReAct (Reasoning + Acting) agent framework with RAG. The model explicitly alternates between Thought (reasoning), Action (retrieval or other tool call), and Observation (processing the result). This structure makes the reasoning process fully transparent and debuggable because each step is explicitly labelled."}),e.jsx("p",{children:'ReAct-RAG is the most general of these architectures — the "action" can be a vector search, a graph query, a web search, or any other tool. It is the foundation for Agentic RAG systems where retrieval is one of many tools an agent can use.'}),e.jsx("h2",{children:"Comparing the Architectures"}),e.jsx("p",{children:"All three architectures interleave reasoning and retrieval, but differ in how retrieval is triggered and how reasoning is structured:"}),e.jsxs("ul",{children:[e.jsxs("li",{children:[e.jsx("strong",{children:"IRCoT"}),": Model-driven. The model's reasoning chain determines when and what to retrieve. Most natural for chain-of-thought capable models."]}),e.jsxs("li",{children:[e.jsx("strong",{children:"FLARE"}),": Probability-driven. Low token confidence triggers retrieval. Requires log-prob access. Best for factual long-form generation."]}),e.jsxs("li",{children:[e.jsx("strong",{children:"ReAct-RAG"}),": Tool-driven. Retrieval is an explicit action in a structured thought/action/observation loop. Most transparent and extensible."]})]}),e.jsx(s,{type:"note",title:"IRCoT is the most practical starting point",children:"FLARE requires log-prob access (not available in all model APIs) and is complex to implement. ReAct-RAG requires building a full agent loop. IRCoT is the most straightforward to implement with standard chat completion APIs — it is essentially prompted chain-of-thought with retrieval calls inserted when the model signals it needs more information. Start with IRCoT for multi-hop retrieval and graduate to ReAct or a full agent framework if you need more flexibility."}),e.jsx(n,{title:"Cap the maximum number of retrieval steps",children:"All iterative multi-hop architectures need a hard cap on the number of retrieval steps. Without a cap, a confused model can loop indefinitely, retrieving documents that never satisfy its stated knowledge gaps. Set a maximum of 3–5 steps for most use cases. After reaching the cap, generate the best answer possible from the accumulated context rather than failing. Log queries that hit the cap — they indicate either unusually complex questions or retrieval quality issues."}),e.jsx("h2",{children:"IRCoT Implementation"}),e.jsx(t,{title:"IRCoT: Interleaved Retrieval with Chain-of-Thought",tabs:{python:q}}),e.jsx("p",{children:"The IRCoT implementation generates reasoning steps one at a time, triggering retrieval when the model identifies a knowledge gap and provides a search query. Each new retrieval adds to the accumulated context, which is included in subsequent reasoning steps. The process terminates when the model produces a final answer or the maximum step count is reached. This architecture naturally produces an interpretable reasoning trace that shows exactly how the model navigated from question to answer across multiple retrieval steps."})]})}const B=Object.freeze(Object.defineProperty({__proto__:null,default:k},Symbol.toStringTag,{value:"Module"})),j=`from anthropic import Anthropic
import json

client = Anthropic()

# ---- Tool Definitions for Agentic RAG ----

TOOLS = [
    {
        "name": "search_knowledge_base",
        "description": "Search the internal knowledge base for relevant documents.",
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "The search query"},
                "filter_source": {"type": "string", "description": "Optional source filter (e.g. 'finance', 'hr')"},
                "top_k": {"type": "integer", "description": "Number of results to return (default 5)"},
            },
            "required": ["query"],
        },
    },
    {
        "name": "search_web",
        "description": "Search the web for current information not in the knowledge base.",
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "The web search query"},
            },
            "required": ["query"],
        },
    },
    {
        "name": "get_document_by_id",
        "description": "Retrieve the full text of a specific document by its ID.",
        "input_schema": {
            "type": "object",
            "properties": {
                "document_id": {"type": "string", "description": "The document ID to retrieve"},
            },
            "required": ["document_id"],
        },
    },
]

# ---- Simulated Tool Handlers ----

def handle_tool_call(tool_name: str, tool_input: dict) -> str:
    """Execute a tool call and return the result as a string."""
    if tool_name == "search_knowledge_base":
        # In production, call your actual vector store
        query = tool_input["query"]
        return json.dumps({
            "results": [
                {"id": "doc_1", "text": f"Relevant result for: {query}", "source": "internal", "score": 0.92},
                {"id": "doc_2", "text": f"Another result for: {query}", "source": "internal", "score": 0.85},
            ]
        })
    elif tool_name == "search_web":
        return json.dumps({"results": [{"text": f"Web result for: {tool_input['query']}", "url": "https://example.com"}]})
    elif tool_name == "get_document_by_id":
        return json.dumps({"id": tool_input["document_id"], "full_text": "Full document content here..."})
    return json.dumps({"error": f"Unknown tool: {tool_name}"})

# ---- Agentic RAG Loop ----

def agentic_rag(user_query: str, max_iterations: int = 5) -> str:
    """
    Agentic RAG: the model uses tools to retrieve information iteratively
    until it has enough context to answer the question.
    """
    messages = [{"role": "user", "content": user_query}]

    system = (
        "You are a research assistant with access to retrieval tools. "
        "Use the tools to find information needed to answer the user's question. "
        "Plan your retrieval strategy, search systematically, and synthesise a "
        "complete answer only after gathering sufficient information. "
        "Cite sources in your final answer."
    )

    for iteration in range(max_iterations):
        response = client.messages.create(
            model="claude-opus-4-5",
            max_tokens=1024,
            system=system,
            tools=TOOLS,
            messages=messages,
        )

        print(f"Iteration {iteration + 1}: stop_reason={response.stop_reason}")

        if response.stop_reason == "end_turn":
            # Model is done — extract the text response
            for block in response.content:
                if hasattr(block, "text"):
                    return block.text
            return "No text response generated."

        elif response.stop_reason == "tool_use":
            # Process all tool calls in this response
            messages.append({"role": "assistant", "content": response.content})
            tool_results = []

            for block in response.content:
                if block.type == "tool_use":
                    print(f"  Tool: {block.name}({json.dumps(block.input)[:80]})")
                    result = handle_tool_call(block.name, block.input)
                    tool_results.append({
                        "type": "tool_result",
                        "tool_use_id": block.id,
                        "content": result,
                    })

            messages.append({"role": "user", "content": tool_results})
        else:
            break

    return "Maximum iterations reached. Best available answer from accumulated context."

# Example
answer = agentic_rag("What are the key differences between HNSW and IVF indexing?")
print(answer)`;function R(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Agentic RAG Overview"}),e.jsx("p",{children:"Standard RAG follows a fixed pipeline: retrieve then generate. The retrieval step is a single, predetermined operation. Agentic RAG replaces this fixed pipeline with an autonomous agent that decides when to retrieve, what to search for, which tools to use, and when it has gathered enough information. This flexibility enables dramatically more sophisticated information gathering than any predefined pipeline."}),e.jsx(r,{term:"Agentic RAG",definition:"Agentic RAG is an approach where a language model acts as an autonomous agent that plans and executes multi-step retrieval strategies using tools. Rather than following a fixed retrieve-then-generate pipeline, the agent decides dynamically how many retrieval steps to perform, which retrieval tools to use, how to refine queries based on intermediate results, and when it has sufficient information to generate a final answer."}),e.jsx(o,{title:"Agentic RAG Architecture",nodes:[{id:"user",label:"User Query",type:"external",x:100,y:200},{id:"agent",label:"LLM Agent",type:"agent",x:350,y:200},{id:"kb",label:"Vector Store",type:"store",x:600,y:100},{id:"web",label:"Web Search",type:"external",x:600,y:200},{id:"graph",label:"Knowledge Graph",type:"store",x:600,y:300},{id:"answer",label:"Final Answer",type:"external",x:850,y:200}],edges:[{from:"user",to:"agent",label:"query"},{from:"agent",to:"kb",label:"search_kb()"},{from:"agent",to:"web",label:"search_web()"},{from:"agent",to:"graph",label:"graph_query()"},{from:"kb",to:"agent",label:"chunks"},{from:"web",to:"agent",label:"results"},{from:"graph",to:"agent",label:"triples"},{from:"agent",to:"answer",label:"synthesised response"}]}),e.jsx("h2",{children:"How Agentic RAG Differs from Pipeline RAG"}),e.jsx("h3",{children:"Dynamic Tool Selection"}),e.jsx("p",{children:"A pipeline RAG system uses the same retrieval method for every query. An agentic RAG system can choose between multiple retrieval tools: vector search for semantic queries, BM25 for keyword-heavy queries, web search for current information, graph queries for relational questions. The agent selects the right tool for each step based on the query's characteristics."}),e.jsx("h3",{children:"Adaptive Retrieval Depth"}),e.jsx("p",{children:"Simple questions terminate after a single retrieval step. Complex questions trigger multiple sequential or parallel retrieval steps. The agent decides how much information it needs rather than being constrained by a fixed k parameter."}),e.jsx("h3",{children:"Self-Directed Query Refinement"}),e.jsx("p",{children:"When initial retrieval returns low-quality results, an agent can reformulate its query, try a different tool, or broaden/narrow the scope. A pipeline cannot do this — if the first retrieval fails, the pipeline continues with bad context."}),e.jsx("h3",{children:"Tool Composition"}),e.jsx("p",{children:"Agents can compose multiple tools in sequence. A complex research query might involve a vector search to find relevant documents, a graph query to expand to related entities, and a web search to supplement with current information. The agent orchestrates these tools based on the information gaps it identifies."}),e.jsx("h2",{children:"The Agentic RAG Loop"}),e.jsx("p",{children:"The core loop of any agentic RAG system:"}),e.jsxs("ol",{children:[e.jsx("li",{children:"The user query arrives. The agent plans its retrieval strategy."}),e.jsx("li",{children:"The agent calls a retrieval tool with a specific query."}),e.jsx("li",{children:"The tool returns results. The agent reads them and decides what to do next."}),e.jsx("li",{children:"If more information is needed, the agent formulates a follow-up query and calls another tool."}),e.jsx("li",{children:"When the agent has sufficient information, it generates a final synthesised answer."})]}),e.jsx("h2",{children:"Implementing with Claude's Tool Use"}),e.jsx("p",{children:"Claude's native tool use API is ideal for agentic RAG. Define retrieval operations as tools with clear names, descriptions, and input schemas. Claude will select which tools to use and in what order based on the query, automatically plan multi-step retrieval strategies, and synthesise a final answer when it has gathered enough information."}),e.jsx(s,{type:"note",title:"Agentic RAG is powerful but more expensive",children:"Each tool call in an agentic loop adds an LLM inference pass and potentially multiple embedding + vector search operations. A simple question might use 1 tool call; a complex research query might use 5–8. At $0.01–0.05 per query for agentic RAG vs. $0.003–0.01 for pipeline RAG, the cost difference is significant at scale. Reserve agentic RAG for queries that genuinely benefit from adaptive retrieval; use pipeline RAG for the majority of straightforward queries."}),e.jsx(n,{title:"Define narrow, well-scoped tools with clear descriptions",children:"Tool description quality is the most important factor in agentic RAG performance. The LLM selects tools based on their descriptions — vague descriptions lead to poor tool selection. Write descriptions that specify exactly what information the tool returns, what queries it is best suited for, and what it cannot do. Include examples in the description if the tool has non-obvious use cases. A well-described narrow tool outperforms a poorly described general tool every time."}),e.jsx("h2",{children:"Agentic RAG with Claude Tool Use"}),e.jsx(t,{title:"Agentic RAG with Claude Tool Use API",tabs:{python:j}}),e.jsx("p",{children:"The implementation defines three retrieval tools (knowledge base search, web search, document fetch) and runs the standard Claude tool-use loop: call the model, process tool calls, return results, repeat until the model stops calling tools and generates a final answer. The agent autonomously decides which tools to use and in what order — you provide the tools and the loop; the model provides the strategy."})]})}const Q=Object.freeze(Object.defineProperty({__proto__:null,default:R},Symbol.toStringTag,{value:"Module"})),A=`from anthropic import Anthropic
import json
import voyageai
import chromadb

client = Anthropic()
voyage = voyageai.Client()
chroma = chromadb.PersistentClient(path="./vector_store")

# ---- Precision-Crafted Retrieval Tools ----

RETRIEVAL_TOOLS = [
    {
        "name": "semantic_search",
        "description": (
            "Search the knowledge base using semantic similarity. "
            "Best for conceptual questions, topic exploration, and when exact keywords may not appear in documents. "
            "Returns ranked document chunks with source citations."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "A clear, specific search query describing the information you need",
                },
                "source_filter": {
                    "type": "string",
                    "description": "Optional: filter results to documents from a specific source (e.g. 'finance', 'hr', 'product')",
                },
                "top_k": {
                    "type": "integer",
                    "description": "Number of results (1-10). Default 5. Use fewer for precision, more for broad coverage.",
                    "minimum": 1,
                    "maximum": 10,
                },
            },
            "required": ["query"],
        },
    },
    {
        "name": "keyword_search",
        "description": (
            "Search using exact keyword matching (BM25). "
            "Best for product names, error codes, version numbers, IDs, or any query requiring exact term matching. "
            "Complements semantic_search for technical queries."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "keywords": {
                    "type": "string",
                    "description": "Space-separated keywords or short phrase to search for",
                },
                "top_k": {"type": "integer", "description": "Number of results (default 5)", "minimum": 1, "maximum": 10},
            },
            "required": ["keywords"],
        },
    },
    {
        "name": "fetch_document",
        "description": (
            "Retrieve the full text of a specific document by its ID. "
            "Use when semantic_search returns a relevant chunk and you need more context from the same document."
        ),
        "input_schema": {
            "type": "object",
            "properties": {
                "document_id": {
                    "type": "string",
                    "description": "The document ID from a previous search result",
                },
            },
            "required": ["document_id"],
        },
    },
    {
        "name": "list_sources",
        "description": "List all available document sources/categories in the knowledge base. Use this first to understand what data is available.",
        "input_schema": {
            "type": "object",
            "properties": {},
        },
    },
]

# ---- Tool Handler Implementations ----

def handle_semantic_search(query: str, source_filter: str = None, top_k: int = 5) -> dict:
    result = voyage.embed([query], model="voyage-3")
    query_vec = result.embeddings[0]

    try:
        collection = chroma.get_collection("knowledge_base")
    except Exception:
        return {"error": "Knowledge base not found. Please initialise the vector store."}

    where = {"source": source_filter} if source_filter else None
    res = collection.query(
        query_embeddings=[query_vec],
        n_results=top_k,
        where=where,
    )

    results = [
        {
            "id": id_,
            "text": doc,
            "source": meta.get("source", "unknown"),
            "relevance_score": round(1 - dist, 3),
        }
        for id_, doc, meta, dist in zip(
            res["ids"][0], res["documents"][0], res["metadatas"][0], res["distances"][0]
        )
    ]
    return {"query": query, "results": results, "total": len(results)}

def handle_list_sources() -> dict:
    try:
        collection = chroma.get_collection("knowledge_base")
        # Sample metadata to get unique sources
        sample = collection.get(limit=1000, include=["metadatas"])
        sources = list(set(m.get("source", "unknown") for m in sample["metadatas"]))
        return {"available_sources": sorted(sources)}
    except Exception:
        return {"available_sources": [], "note": "Knowledge base not initialised"}

def dispatch_tool(tool_name: str, tool_input: dict) -> str:
    if tool_name == "semantic_search":
        result = handle_semantic_search(
            query=tool_input["query"],
            source_filter=tool_input.get("source_filter"),
            top_k=tool_input.get("top_k", 5),
        )
    elif tool_name == "keyword_search":
        # Simplified BM25 — replace with real BM25 implementation
        result = {"results": [], "note": "BM25 stub — implement with rank_bm25"}
    elif tool_name == "fetch_document":
        result = {"id": tool_input["document_id"], "full_text": "Full document text here..."}
    elif tool_name == "list_sources":
        result = handle_list_sources()
    else:
        result = {"error": f"Unknown tool: {tool_name}"}
    return json.dumps(result)

# ---- Agentic Retrieval Loop ----

def run_retrieval_agent(question: str) -> str:
    messages = [{"role": "user", "content": question}]
    system = (
        "You are a precise research assistant. Use the provided retrieval tools systematically "
        "to find information. Start by listing sources if you are unsure where to look. "
        "Use semantic_search for conceptual queries and keyword_search for technical terms. "
        "Fetch full documents when you need more context beyond a retrieved chunk. "
        "Synthesise a complete, cited answer once you have sufficient information."
    )

    for _ in range(8):  # max 8 iterations
        response = client.messages.create(
            model="claude-opus-4-5",
            max_tokens=1024,
            system=system,
            tools=RETRIEVAL_TOOLS,
            messages=messages,
        )

        if response.stop_reason == "end_turn":
            return next((b.text for b in response.content if hasattr(b, "text")), "")

        messages.append({"role": "assistant", "content": response.content})
        tool_results = []
        for block in response.content:
            if block.type == "tool_use":
                result = dispatch_tool(block.name, block.input)
                tool_results.append({
                    "type": "tool_result",
                    "tool_use_id": block.id,
                    "content": result,
                })
        messages.append({"role": "user", "content": tool_results})

    return "Research complete. Generating best available answer."`;function T(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"Tool-Use in Retrieval"}),e.jsx("p",{children:"The quality of an agentic RAG system is largely determined by the quality of its retrieval tools. A vaguely described tool leads to imprecise tool calls; an overly broad tool forces the agent to retrieve more than it needs. Designing narrow, well-specified retrieval tools with clear descriptions is the highest- leverage engineering decision in agentic RAG."}),e.jsx(r,{term:"Retrieval Tool",definition:"In agentic RAG, a retrieval tool is a callable function exposed to the LLM agent with a schema that specifies its name, description, and input parameters. The agent reads the tool description to decide when to use it and what inputs to provide. Precision-crafted retrieval tools with clear descriptions, explicit use-case guidance, and well-defined parameters enable agents to issue targeted, effective retrieval calls rather than generic broad searches."}),e.jsx("h2",{children:"Tool Design Principles"}),e.jsx("h3",{children:"Single Responsibility"}),e.jsx("p",{children:'Each tool should do one thing well. Separate semantic search, keyword search, and document fetch into distinct tools rather than combining them into one "search" tool. The agent selects the right specialised tool for each step rather than guessing which search modality a combined tool will use.'}),e.jsx("h3",{children:"Explicit Use-Case Guidance in Descriptions"}),e.jsx("p",{children:"The tool description is how the LLM agent decides when to use it. Include:"}),e.jsxs("ul",{children:[e.jsx("li",{children:"What type of queries this tool excels at."}),e.jsx("li",{children:"When to prefer this tool over alternatives."}),e.jsx("li",{children:"What format the results are in."}),e.jsx("li",{children:'Any limitations (e.g., "does not search web content").'})]}),e.jsx("h3",{children:"Granular Input Parameters"}),e.jsx("p",{children:'Break search inputs into specific parameters: separate query string from metadata filters, explicit top_k with documented constraints, optional parameters with clear defaults. Granular inputs give the agent precise control and produce more targeted retrieval calls than a single broad "query" parameter.'}),e.jsx("h3",{children:"Structured Return Values"}),e.jsx("p",{children:"Return results as structured JSON with consistent field names: id, text, source, relevance_score. Consistent structure lets the agent reliably parse and reason about results across multiple tool calls. Include document IDs so the agent can fetch full documents when it needs more context from a partial chunk."}),e.jsx("h2",{children:"The list_sources Tool Pattern"}),e.jsxs("p",{children:["A particularly useful tool for knowledge bases with multiple data domains is a",e.jsx("code",{children:"list_sources"}),' tool that returns all available document categories. An agent querying "What does the HR policy say about remote work?" can first call',e.jsx("code",{children:"list_sources"}),' to confirm there is an "hr" source, then issue a targeted',e.jsx("code",{children:"semantic_search"})," with ",e.jsx("code",{children:'source_filter: "hr"'}),'. Without this discovery step, the agent may search across all sources and receive results from finance or product documents that happen to mention "remote."']}),e.jsx("h2",{children:"Parallel Tool Calls"}),e.jsx("p",{children:"Claude can issue multiple tool calls in a single response turn when the calls are independent. For a query that requires both semantic and keyword retrieval, the agent can call both tools simultaneously rather than sequentially. This halves the latency of the retrieval step. Design your tools to be stateless so they can safely run in parallel."}),e.jsx(s,{type:"tip",title:"Log tool call sequences to understand agent behaviour",children:"In production, log the full sequence of tool calls an agent makes for each query: tool name, input parameters, result size, and timestamps. Analysing these logs reveals which tools are used most, which queries trigger many retrieval iterations, and where the agent's search strategy breaks down. This data is invaluable for improving tool descriptions and identifying retrieval gaps in your knowledge base."}),e.jsx(n,{title:"Test each tool in isolation before testing the full agent",children:"Before testing the agentic RAG loop end-to-end, test each retrieval tool individually: verify that semantic_search returns relevant results for representative queries, that keyword_search finds exact technical terms, and that fetch_document returns complete document text. Tool-level testing catches configuration issues (wrong collection names, missing metadata fields) that are much harder to diagnose inside a multi-step agent loop."}),e.jsx("h2",{children:"Precision-Crafted Retrieval Tools in Practice"}),e.jsx(t,{title:"Precision-Crafted Retrieval Tools for Agents",tabs:{python:A}}),e.jsxs("p",{children:["The example above defines four tools with explicit, differentiated descriptions: semantic search (with source filter and top_k parameters), keyword search (for exact terms), document fetch (for full context), and source listing (for discovery). The tool descriptions guide the agent to choose the right tool for each information need. The ",e.jsx("code",{children:"run_retrieval_agent"})," function implements the standard tool- use loop and dispatches each tool call to its specific handler."]})]})}const z=Object.freeze(Object.defineProperty({__proto__:null,default:T},Symbol.toStringTag,{value:"Module"})),G=`from anthropic import Anthropic
from typing import TypedDict, Annotated
import json
import operator

client = Anthropic()

# ---- State and Tools ----

class RAGState(TypedDict):
    question: str
    retrieved_chunks: list[dict]
    search_queries: list[str]
    answer: str
    iteration: int
    finished: bool

TOOLS = [
    {
        "name": "vector_search",
        "description": "Search the knowledge base for relevant document chunks.",
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string"},
                "top_k": {"type": "integer", "default": 5},
            },
            "required": ["query"],
        },
    },
    {
        "name": "generate_final_answer",
        "description": "Generate the final answer when you have sufficient context. Call this when ready to respond.",
        "input_schema": {
            "type": "object",
            "properties": {
                "answer": {"type": "string", "description": "The final synthesised answer with citations"},
            },
            "required": ["answer"],
        },
    },
]

# ---- Simulated Vector Search ----

def vector_search(query: str, top_k: int = 5) -> list[dict]:
    # In production: embed query and search your vector store
    return [
        {"id": f"doc_{i}", "text": f"Result {i} for: {query}", "source": f"source_{i}", "score": 0.9 - i*0.05}
        for i in range(min(top_k, 3))
    ]

# ---- LangGraph-style Agent Nodes ----

def retrieve_node(state: RAGState) -> RAGState:
    """
    Retrieval node: the agent plans and executes retrieval using tools.
    Runs until the agent decides it has enough context or calls generate_final_answer.
    """
    messages = [
        {
            "role": "user",
            "content": (
                f"Question: {state['question']}\\n\\n"
                f"{'Previously retrieved chunks: ' + json.dumps(state['retrieved_chunks'][:3]) if state['retrieved_chunks'] else 'No chunks retrieved yet.'}"
                f"\\n\\nPlan your retrieval and search for the information you need. "
                f"When you have enough information, call generate_final_answer."
            )
        }
    ]

    system = (
        "You are a research agent. Use vector_search to find relevant information. "
        "Search with different queries to get comprehensive coverage. "
        "When you have enough context, call generate_final_answer with your synthesised response."
    )

    new_chunks = list(state["retrieved_chunks"])
    final_answer = ""

    for _ in range(4):  # max sub-iterations per node
        response = client.messages.create(
            model="claude-opus-4-5",
            max_tokens=1024,
            system=system,
            tools=TOOLS,
            messages=messages,
        )

        if response.stop_reason == "end_turn":
            break

        messages.append({"role": "assistant", "content": response.content})
        tool_results = []

        for block in response.content:
            if block.type == "tool_use":
                if block.name == "vector_search":
                    results = vector_search(block.input["query"], block.input.get("top_k", 5))
                    new_chunks.extend(results)
                    tool_results.append({
                        "type": "tool_result",
                        "tool_use_id": block.id,
                        "content": json.dumps({"results": results}),
                    })
                elif block.name == "generate_final_answer":
                    final_answer = block.input["answer"]
                    tool_results.append({
                        "type": "tool_result",
                        "tool_use_id": block.id,
                        "content": '{"status": "answer_recorded"}',
                    })

        messages.append({"role": "user", "content": tool_results})

        if final_answer:
            break

    return {
        **state,
        "retrieved_chunks": new_chunks,
        "answer": final_answer,
        "finished": bool(final_answer),
        "iteration": state["iteration"] + 1,
    }

def grade_node(state: RAGState) -> RAGState:
    """
    Grading node: evaluate whether the answer is complete and grounded.
    If not, mark as not finished to trigger another retrieval iteration.
    """
    if not state["answer"]:
        return {**state, "finished": False}

    response = client.messages.create(
        model="claude-opus-4-5",
        max_tokens=128,
        system='Evaluate answer quality. Return JSON: {"complete": true|false, "grounded": true|false}',
        messages=[{
            "role": "user",
            "content": (
                f"Question: {state['question']}\\n\\n"
                f"Answer: {state['answer']}\\n\\n"
                "Is this answer complete and grounded in the retrieved chunks?"
            )
        }],
    )
    raw = response.content[0].text.strip().strip("json").strip("")
    evaluation = json.loads(raw)

    is_done = evaluation.get("complete") and evaluation.get("grounded")
    return {**state, "finished": is_done or state["iteration"] >= 3}

def run_agentic_rag_graph(question: str) -> dict:
    """Run the agentic RAG workflow as a simple state machine."""
    state: RAGState = {
        "question": question,
        "retrieved_chunks": [],
        "search_queries": [],
        "answer": "",
        "iteration": 0,
        "finished": False,
    }

    while not state["finished"] and state["iteration"] < 3:
        state = retrieve_node(state)
        state = grade_node(state)
        print(f"Iteration {state['iteration']}: finished={state['finished']}, "
              f"chunks={len(state['retrieved_chunks'])}")

    return {"question": question, "answer": state["answer"], "iterations": state["iteration"]}

# Example
result = run_agentic_rag_graph("What are the key differences between HNSW and IVF indexing?")
print(result["answer"])`;function S(){return e.jsxs("div",{className:"lesson-content",children:[e.jsx("h2",{children:"RAG with Agents"}),e.jsx("p",{children:"The previous sections introduced agentic RAG concepts and tool design. This section shows how to wire a complete RAG pipeline into a production agent loop using LangGraph-style state machines or custom orchestration. The result is a system where retrieval, grading, and generation are discrete, composable nodes that can be tested, monitored, and extended independently."}),e.jsx(r,{term:"RAG Agent Graph",definition:"A RAG agent graph is a directed workflow where each node performs a specific step in the retrieval-augmented generation process: query planning, retrieval, document grading, answer generation, and quality evaluation. Edges between nodes can be conditional — the workflow routes to different nodes based on the output of each step. This structure makes complex agentic RAG pipelines explicit, debuggable, and composable."}),e.jsx("h2",{children:"The Node Architecture"}),e.jsx("h3",{children:"Retrieval Node"}),e.jsxs("p",{children:["The retrieval node is an agent loop in itself: the LLM uses retrieval tools to gather information, reformulating queries as needed, until it decides it has enough context. It terminates by calling a special ",e.jsx("code",{children:"generate_final_answer"})," tool (or equivalent signal), passing the synthesised answer forward to the next node. This design makes retrieval self-contained and testable."]}),e.jsx("h3",{children:"Grade Node"}),e.jsx("p",{children:"The grade node evaluates the answer produced by the retrieval node. It checks whether the answer is complete (addresses all aspects of the question) and grounded (supported by retrieved context). If the answer fails the grade, the workflow loops back to the retrieval node for another iteration with the question and previous answer in context. This implements automatic quality control."}),e.jsx("h3",{children:"Generate Node"}),e.jsx("p",{children:"In some architectures, generation is separated from retrieval: the retrieval node only gathers context, and a separate generation node synthesises the final answer. This separation allows different models to be used for retrieval planning (cheaper, faster) and final answer generation (highest quality). It also allows the generation step to be retried without repeating retrieval."}),e.jsx("h2",{children:"Implementing with LangGraph"}),e.jsx("p",{children:"LangGraph is a library for building multi-agent workflows as state graphs. A RAG agent graph in LangGraph defines:"}),e.jsxs("ul",{children:[e.jsxs("li",{children:[e.jsx("strong",{children:"State"}),": A typed dict containing the question, retrieved chunks, search history, answer, and quality flags."]}),e.jsxs("li",{children:[e.jsx("strong",{children:"Nodes"}),": Python functions that take and return state."]}),e.jsxs("li",{children:[e.jsx("strong",{children:"Edges"}),": Connections between nodes, including conditional edges that route based on state values."]}),e.jsxs("li",{children:[e.jsx("strong",{children:"Entry and exit points"}),": Where the workflow starts and what constitutes a terminal state."]})]}),e.jsx("p",{children:"The code below implements the same architecture without the LangGraph dependency — using a simple state dict and a while loop — to show the underlying logic clearly. In production, LangGraph provides streaming, persistence, and multi-agent orchestration built-in."}),e.jsx("h2",{children:"Custom Orchestration Patterns"}),e.jsx("p",{children:"Not every team needs LangGraph. For simpler agentic RAG patterns, a standard while-loop with the Claude tool-use API is entirely sufficient:"}),e.jsxs("ul",{children:[e.jsx("li",{children:"Maintain a messages list that grows with each tool call and result."}),e.jsxs("li",{children:["Check ",e.jsx("code",{children:"response.stop_reason"}),' — if "tool_use", execute tools and continue; if "end_turn", extract the final answer.']}),e.jsx("li",{children:"Track state (retrieved chunks, queries issued, iteration count) in a local dict."}),e.jsx("li",{children:"Apply a hard iteration cap and a quality evaluation step after each cycle."})]}),e.jsx(s,{type:"tip",title:"Add streaming for better user experience",children:"The agentic RAG loop can take 5–30 seconds for complex queries with multiple retrieval steps. Stream intermediate status updates to the user: 'Searching knowledge base...', 'Found 3 relevant documents, searching for more...', 'Generating answer...'. Claude's streaming API supports streaming content blocks including tool calls, allowing you to detect when tools are being called and show progress messages before the final answer arrives."}),e.jsx(n,{title:"Implement a feedback loop from failed grades to retrieval",children:`When the grade node determines that an answer is incomplete or ungrounded, the workflow loops back to retrieval. Make the grade node's failure reason explicit in the state — "answer missing information about X" or "claim Y is not supported by retrieved context" — and include this feedback in the next retrieval node's system prompt. This targeted feedback dramatically improves the retrieval node's ability to fill the specific gap on the second attempt.`}),e.jsx("h2",{children:"Complete Agentic RAG with State Machine"}),e.jsx(t,{title:"RAG Pipeline Wired into an Agent Loop",tabs:{python:G}}),e.jsxs("p",{children:["The implementation combines a retrieval agent node (which uses tools to gather context until it generates an answer) with a grading node (which evaluates completeness and groundedness). The ",e.jsx("code",{children:"run_agentic_rag_graph"})," function runs the state machine until the grade passes or the iteration limit is reached. This pattern scales from a simple two-node graph to a complex multi-agent workflow by adding more specialised nodes — a planner node, a fact-checker node, a citation formatter node — as your requirements evolve."]})]})}const V=Object.freeze(Object.defineProperty({__proto__:null,default:S},Symbol.toStringTag,{value:"Module"}));export{L as a,M as b,P as c,O as d,D as e,F as f,N as g,H as h,W as i,U as j,B as k,Q as l,z as m,V as n,E as s};
